<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>Brain=f(X)?</title><link>https://dreamanddead.github.io/</link><description>Recent content on Brain=f(X)?</description><generator>Hugo -- gohugo.io</generator><language>zh-cn</language><copyright>Attribution-NonCommercial-ShareAlike 4.0 International</copyright><lastBuildDate>Mon, 26 Oct 2020 09:10:00 +0000</lastBuildDate><atom:link href="https://dreamanddead.github.io/index.xml" rel="self" type="application/rss+xml"/><item><title>Recover mysql table structure from frm file</title><link>https://dreamanddead.github.io/post/recover-mysql-table-structure-from-frm-file/</link><pubDate>Thu, 27 Sep 2018 10:20:00 +0000</pubDate><guid>https://dreamanddead.github.io/post/recover-mysql-table-structure-from-frm-file/</guid><description>Brain=f(X)? https://dreamanddead.github.io/post/recover-mysql-table-structure-from-frm-file/ -&lt;p>mysql 正常运行的时候，&lt;a href="https://dev.mysql.com/doc/refman/8.0/en/show-create-table.html">查看 table 的结构&lt;/a>并不是困难的事。
但是有时 mysql 发生故障，这种方法便不再可行。&lt;/p>
&lt;p>当遇到故障，通常使用新的 mysql 实例来恢复当前的数据。
建表是非常重要的步骤，我们必须有其它的方法来寻找 table 的结构。&lt;/p>
&lt;h1 id="table-结构定义在哪里">table 结构定义在哪里&lt;/h1>
&lt;p>通常关注的用户数据，底层都实际存储在 mysql 数据目录。
其它的元数据也不例外，比如 table 表结构的定义。&lt;/p>
&lt;p>mysql 数据目录文件结构是非常清晰的，&lt;/p>
&lt;ul>
&lt;li>目录对应数据库&lt;/li>
&lt;li>frm 文件存储了 table 结构的定义&lt;/li>
&lt;li>ibdata 文件存储了 mysql 的元数据及其它&lt;/li>
&lt;/ul>
&lt;p>table 定义的结构，就存在于 frm 文件中，当然管理元数据的 ibdata 也会有记录。&lt;/p>
&lt;p>当存在 frm 文件的时候，恢复表结构相对容易；
但是如果执行了 drop table，便删除了 frm 文件，本文所提供的方法就爱莫能助了。
这种情况下，可以尝试从 ibdata 恢复表结构，这暂时不在下面的讨论范围内。&lt;/p>
&lt;h1 id="解析-table-结构">解析 table 结构&lt;/h1>
&lt;p>下面介绍 3 种方式，从 frm 文件中，解析得到 &lt;code>create table&lt;/code> 命令。&lt;/p>
&lt;h2 id="mysqlfrm">mysqlfrm&lt;/h2>
&lt;p>mysqlfrm 是 &lt;a href="https://downloads.mysql.com/archives/utilities/">mysql utilities 工具集&lt;/a> 中的其中之一，
用于分析 frm 文件生成 create table 命令。
目前已经不再更新，部分功能并入了新版本的 mysql shell（version 8 及以后）。&lt;/p>
&lt;p>mysql utilities 需要 python2 环境，安装非常简单。&lt;/p>
&lt;pre>&lt;code>$ tar -xvzf mysql-utilities-1.6.5.tar.gz
$ cd mysql-utilities-1.6.5
$ python setup.py build
$ python setup.py install
&lt;/code>&lt;/pre>&lt;p>mysqlfrm 支持两类模式来解读 frm：&lt;/p>
&lt;h3 id="直接分析">直接分析&lt;/h3>
&lt;p>这种模式比较直接，逐个字节分析 frm 文件，尽可能的提取信息。&lt;/p>
&lt;p>这种模式下，需要使用 &lt;code>--diagnostic&lt;/code> 参数。&lt;/p>
&lt;pre>&lt;code>$ mysqlfrm --diagnostic /data/sakila/actor.frm
&lt;/code>&lt;/pre>&lt;h3 id="借助-mysql-实例分析">借助 mysql 实例分析&lt;/h3>
&lt;p>这种模式，借助新的 mysql 实例，从中完成 frm 的分析工作。
可以用两种方式来指定，如何开启新的 mysql 实例。&lt;/p>
&lt;p>一，从当前的 mysql 服务中 spawn，使用 &lt;code>--server&lt;/code> 指定 mysql 服务&lt;/p>
&lt;pre>&lt;code>$ mysqlfrm --server=root:pass@localhost:3306 --port=3310 /data/sakila/actor.frm
&lt;/code>&lt;/pre>&lt;p>二，启动新的 mysql 实例，使用 &lt;code>--basedir&lt;/code> 指定 mysql 程序路径&lt;/p>
&lt;pre>&lt;code>$ mysqlfrm --basedir=/usr/local/bin/mysql --port=3310 /data/sakila/actor.frm
&lt;/code>&lt;/pre>&lt;p>&lt;code>--port&lt;/code> 给新的实例指定端口，是为了避免与当前的 3306 端口出现冲突。&lt;/p>
&lt;h2 id="dbsake">dbsake&lt;/h2>
&lt;p>这是偶然发现的一个工具，文档中它这样介绍自己：&lt;/p>
&lt;blockquote>
&lt;p>dbsake - a (s)wiss-(a)rmy-(k)nif(e) for MySQL&lt;/p>
&lt;/blockquote>
&lt;p>作者一定是一个对 mysql 很有心得的人，
工具从下载，安装到使用，简单，利落。&lt;/p>
&lt;pre>&lt;code>$ curl -s get.dbsake.net &amp;gt; dbsake
$ chmod u+x dbsake
$ ./dbsake frmdump [frm-file-path]
&lt;/code>&lt;/pre>&lt;h2 id="online-service">online service&lt;/h2>
&lt;p>有一些在线的服务，也关注这样的问题。
使用过的&lt;a href="https://recovery.twindb.com/">twindb online&lt;/a>，体验非常好，相关的工具集也很棒。&lt;/p>
&lt;p>从 &lt;code>Recover Structure -&amp;gt; from .frm file&lt;/code> 入口，上传 frm，就可以得到 &lt;code>create table&lt;/code> 命令。&lt;/p>
&lt;h1 id="写在最后">写在最后&lt;/h1>
&lt;p>在使用上，可以多个工具都测试一下，对比哪个工具恢复的命令更为完善可取，选择最佳的。&lt;/p>
&lt;p>参考：&lt;/p>
&lt;ul>
&lt;li>&lt;a href="https://docs.oracle.com/cd/E17952_01/mysql-utilities-1.6-en/mysqlfrm.html">mysqlfrm official doc&lt;/a>&lt;/li>
&lt;li>&lt;a href="https://dbsake.readthedocs.io/en/latest/commands/frmdump.html">dbsake project doc&lt;/a>&lt;/li>
&lt;/ul>- https://dreamanddead.github.io/post/recover-mysql-table-structure-from-frm-file/ - Attribution-NonCommercial-ShareAlike 4.0 International</description></item><item><title>mysql table suddenly doesn't exists</title><link>https://dreamanddead.github.io/post/recover-mysql-innodb-table-data-from-ibd-file/</link><pubDate>Thu, 27 Sep 2018 04:10:00 +0000</pubDate><guid>https://dreamanddead.github.io/post/recover-mysql-innodb-table-data-from-ibd-file/</guid><description>Brain=f(X)? https://dreamanddead.github.io/post/recover-mysql-innodb-table-data-from-ibd-file/ -&lt;p>最近内部的 mysql 数据库发生了一件奇怪的事，其中有一个表 users625 突然出现问题，
所有对它的操作都报错误 &lt;code>数据表不存在&lt;/code>。&lt;/p>
&lt;pre>&lt;code>mysql&amp;gt; select count(*) from users625;
ERROR 1146 (42S02): Table 'km8.users625' doesn't exist
&lt;/code>&lt;/pre>&lt;p>&lt;code>show tables&lt;/code> 它还显示在列表里，在 mysql 数据目录中也可以找到对应的表文件，也没有
进行过删除操作，突然出现这样的错误非常奇怪。&lt;/p>
&lt;p>内部运行环境：&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th style="text-align:center">名称&lt;/th>
&lt;th style="text-align:center">值&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td style="text-align:center">OS&lt;/td>
&lt;td style="text-align:center">Debian Squeeze x64&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td style="text-align:center">mysql 版本&lt;/td>
&lt;td style="text-align:center">5.1&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td style="text-align:center">mysql 引擎&lt;/td>
&lt;td style="text-align:center">innodb&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h1 id="发生了什么">发生了什么&lt;/h1>
&lt;p>突然出现这种情况，第一反应必定是想办法将表中的用户数据找回，但是目前发生问题的情况与原因都不明晰，
不能轻举妄动。&lt;/p>
&lt;p>查看 mysql 日志，在操作出错的时候，日志这样显示：&lt;/p>
&lt;pre>&lt;code>mysqld: 180926 11:10:53 InnoDB: cannot calculate statistics for table km8/users625
mysqld: InnoDB: because the .ibd file is missing. For help, please refer to
mysqld: InnoDB: http://dev.mysql.com/doc/refman/5.1/en/innodb-troubleshooting.html
mysqld: 180926 11:10:53 [ERROR] MySQL is trying to open a table handle but the .ibd file for
mysqld: table km8/users625 does not exist.
mysqld: Have you deleted the .ibd file from the database directory under
mysqld: the MySQL datadir, or have you used DISCARD TABLESPACE?
mysqld: See http://dev.mysql.com/doc/refman/5.1/en/innodb-troubleshooting.html
mysqld: how you can resolve the problem.
&lt;/code>&lt;/pre>&lt;p>其中提到3个可追溯的点：&lt;/p>
&lt;ul>
&lt;li>ibd file&lt;/li>
&lt;li>DISCARD TABLESPACE&lt;/li>
&lt;li>&lt;a href="http://dev.mysql.com/doc/refman/5.1/en/innodb-troubleshooting.html">http://dev.mysql.com/doc/refman/5.1/en/innodb-troubleshooting.html&lt;/a>&lt;/li>
&lt;/ul>
&lt;p>了解这3点提到的内容，应该对判断情况有很好的帮助。&lt;/p>
&lt;h2 id="ibd-file">ibd file&lt;/h2>
&lt;p>日志中提问，是否丢失了 ibd 文件？先到 mysql 数据目录下查找，&lt;/p>
&lt;pre>&lt;code>.
├── ibdata1
├── .......
├── .......
└── km8
├── ............
├── ............
├── users625.frm
├── users625.ibd
├── ............
└── ............
&lt;/code>&lt;/pre>&lt;p>users625 的 ibd 文件是存在的，与之一起的还有文件 users625.frm 。&lt;/p>
&lt;p>根据官方文档&lt;a href="https://dev.mysql.com/doc/internals/en/frm-file-format.html">对 frm 文件的描述&lt;/a>，frm 文件是用来保存 table 表结构（即 table 的定义）的，无论使用什么存储引擎。&lt;/p>
&lt;p>与之相对的，ibd 文件是用来存储表数据（即行数据）的，通常情况下，所有数据都会存储在系统的 ibd 文件，
但是当开启选项 &lt;a href="https://dev.mysql.com/doc/refman/5.5/en/innodb-parameters.html#sysvar_innodb_file_per_table">innodb_file_per_table&lt;/a> 的时候，每个表的数据会使用单独的 ibd 文件来存储。&lt;/p>
&lt;p>当前的 mysql 就开启了这个选项，&lt;/p>
&lt;pre>&lt;code>[mysqld]
innodb_file_per_table=1
&lt;/code>&lt;/pre>&lt;p>目前 frm 与 ibd 文件都存在，从中恢复数据便存在一些希望。&lt;/p>
&lt;h2 id="discard-tablespace">DISCARD TABLESPACE&lt;/h2>
&lt;p>日志中提到的 &lt;code>DISCARD TABLESPACE&lt;/code> 其实是在猜测导致 ibd 文件丢失的原因，因为它会删除相应 table 的 ibd 文件（所谓 tablespace）。&lt;/p>
&lt;pre>&lt;code>&amp;gt; ALTER TABLE km8.users625 DISCARD TABLESPACE;
&lt;/code>&lt;/pre>&lt;p>底层的 users625.ibd 文件就会被删除，丢失所有表数据。&lt;/p>
&lt;p>根据目前情况来看， ibd 文件还存在，所以它不是导致错误的原因。&lt;/p>
&lt;h2 id="trouble-shooting-doc">trouble shooting doc&lt;/h2>
&lt;p>&lt;a href="http://dev.mysql.com/doc/refman/5.1/en/innodb-troubleshooting.html">日志中提到的参考链接&lt;/a>，其中列举了多种情况，和当前问题相关的是一个&lt;a href="https://dev.mysql.com/doc/refman/8.0/en/innodb-troubleshooting-datadict.html">子链接&lt;/a>，
按照它提供的方法，尝试进行数据恢复。&lt;/p>
&lt;h1 id="数据恢复">数据恢复&lt;/h1>
&lt;p>官方文档提到的&lt;a href="https://dev.mysql.com/doc/refman/8.0/en/innodb-troubleshooting-datadict.html">恢复数据&lt;/a>的方法，思路很清晰：&lt;/p>
&lt;ol>
&lt;li>启用相同版本的 mysql 实例（启用选项 innodb_file_per_table）&lt;/li>
&lt;li>建立同样结构的数据表&lt;/li>
&lt;li>替换 ibd 文件（保持文件权限一致）&lt;/li>
&lt;li>导入 ibd 文件中的数据&lt;/li>
&lt;li>使用 mysqldump，导出数据&lt;/li>
&lt;li>将导出的数据导入原数据库&lt;/li>
&lt;/ol>
&lt;p>我按照这种方式尝试恢复数据，并不是那么顺利：&lt;/p>
&lt;h2 id="如何获得-table-表结构">如何获得 table 表结构？&lt;/h2>
&lt;p>在第2步，需要建立同样结构的数据表，目前只有 frm 和 ibd 文件，怎么样得到 &lt;code>create table&lt;/code> 命令？&lt;/p>
&lt;p>根据底层数据存储的理解，table 表结构存储在 frm 文件中，而目前已经有&lt;a href="https://www.cnblogs.com/dreamanddead/p/recover-mysql-table-structure-from-frm-file.html">相应的方法&lt;/a>从中提取出 &lt;code>create table&lt;/code> 命令，
这样就可以用于在新的 mysql 实例中建立 table 。&lt;/p>
&lt;h2 id="tablespace-id-不对应">tablespace id 不对应？&lt;/h2>
&lt;p>在第4步，尝试导入数据的时候，&lt;/p>
&lt;pre>&lt;code>&amp;gt; ALTER TABLE km8.users625 IMPORT TABLESPACE;
ERROR 1030 (HY000): Got error -1 from storage engine
&lt;/code>&lt;/pre>&lt;p>总是出现失败，同时在 mysql 新实例的日志中发现这样的错误：&lt;/p>
&lt;pre>&lt;code>mysqld: InnoDB: Error: tablespace id in file './km8/users625.ibd' is 18446744073709551615, but in the InnoDB
mysqld: InnoDB: data dictinary it is 1.
&lt;/code>&lt;/pre>&lt;p>原来在内部，ibd 文件本身有一个 id，必须和 mysql innodb 内部的 table 元数据相对应，才可以进行导入。&lt;/p>
&lt;p>根据错误信息搜索到&lt;a href="http://www.chriscalender.com/recovering-an-innodb-table-from-only-an-ibd-file/">一篇文章&lt;/a>，其中提到两种办法：&lt;/p>
&lt;ol>
&lt;li>重复建表，因为 mysql 内部的 tablespace id 是累计递增的，预先建立 （18446744073709551615 - 1）张表，再建立
users625 表，就可以对应 id，并进行导入。&lt;/li>
&lt;li>修改 ibd 文件，因为 tablespace id 存储于 ibd 文件，找到它并将其修改为 1，使之与内部的 id 对应，就可以进行导入。&lt;/li>
&lt;/ol>
&lt;p>考虑第 1 种方法，要预先建立上亿张空表？！这根本不可能。&lt;/p>
&lt;p>于是尝试第 2 种方法，研究 ibd 的文件格式，修改对应 id。&lt;/p>
&lt;p>用二进制编辑器打开 users625.ibd 文件，&lt;/p>
&lt;p>&lt;img src="http://on7blnbb0.bkt.clouddn.com/18-9-27/5492997.jpg" alt="users625-ibd-hexdump">&lt;/p>
&lt;pre>&lt;code>18:26:08 UTC - mysqld got signal 6. This could be because you hit a bug. It is also possible that this
binary or one of the libraries it was linked against is corrupt, improperly built or misconfigured.
This error can also be cuased by malfunctioning hardware.
&lt;/code>&lt;/pre>&lt;p>不敢相信自己的眼睛，居然有错误 log 在二进制文件里？！ibd 的文件格式可没有这么说明过。&lt;/p>
&lt;p>随便找一个邻居表正常的 ibd 文件作对比，&lt;/p>
&lt;p>&lt;img src="http://on7blnbb0.bkt.clouddn.com/18-9-27/50479657.jpg" alt="normal-ibd-hexdump">&lt;/p>
&lt;p>看来是出现了 bug ，崩溃的环境直接将数据文件给毁了，这也解释了为什么 tablespace id 会那么大，因为 log
覆盖了原本的 id 字段，使 mysql 解读出了一个好笑的数字。&lt;/p>
&lt;h2 id="暂时放弃">暂时放弃&lt;/h2>
&lt;p>这种情况下，还没有办法将数据恢复回来，只能暂时将表删除，新建空表，保证上层应用程序可以运行。
将 ibd 文件备份下来，看后续还没有其它的办法将其恢复。&lt;/p>
&lt;h1 id="检测所有-table-状态">检测所有 table 状态&lt;/h1>
&lt;p>当前只发现一个出现问题的 table ，可能同时也有其它的 table 出现问题。对此需要做一个全面的检测，
检测有没有其它的表受到牵连。&lt;/p>
&lt;pre>&lt;code>$ mysqlcheck --all-databases
&lt;/code>&lt;/pre>&lt;h1 id="写在最后">写在最后&lt;/h1>
&lt;p>数据库的备份是非常重要的！直接导入备份数据，是解决问题最保险最便捷的办法。
如果没有备份，遇到 bug 丢失数据，只能怪时运不济。&lt;/p>
&lt;p>同时数据库也最好选择稳定的版本，降低出现 bug 的概率。&lt;/p>- https://dreamanddead.github.io/post/recover-mysql-innodb-table-data-from-ibd-file/ - Attribution-NonCommercial-ShareAlike 4.0 International</description></item><item><title>Configure apt rightly for debian squeeze</title><link>https://dreamanddead.github.io/post/configure-debian-squeeze-apt-source/</link><pubDate>Wed, 26 Sep 2018 16:50:00 +0000</pubDate><guid>https://dreamanddead.github.io/post/configure-debian-squeeze-apt-source/</guid><description>Brain=f(X)? https://dreamanddead.github.io/post/configure-debian-squeeze-apt-source/ -&lt;p>本想在 Debian Squeeze 上安装一些依赖，没想到刚执行 &lt;code>apt-get update&lt;/code> ，就出现这样的错误信息。&lt;/p>
&lt;pre>&lt;code>W: GPG error: http://mirrors.163.com squeeze Release: The following signatures were invalid: KEYEXPIRED 1520281423 KEYEXPIRED 1501892461
E: Release file expired, ignoring http://mirrors.163.com/debian-archive/dists/squeeze-lts/Release (invalid since 923d 19h 26min 3s)
&lt;/code>&lt;/pre>&lt;h1 id="问题出在哪里">问题出在哪里&lt;/h1>
&lt;p>Debian Squeeze 6.0 版本对于当前最新的 9.5 版本来说，是过于陈旧了。
错误信息表示，证书过期，也不是太意外的事。&lt;/p>
&lt;p>以 &lt;code>The following signatures were invalid&lt;/code> 为关键字进行搜索，大体有两种方案：&lt;/p>
&lt;h2 id="缓存原因">缓存原因？&lt;/h2>
&lt;p>搜索到的大多数结果给出这样的解答，&lt;a href="https://www.cnblogs.com/arrongao/archive/2012/12/13/2815614.html">比如这里&lt;/a>&lt;/p>
&lt;pre>&lt;code>sudo apt-get clean
cd /var/lib/apt
sudo mv lists lists.old
sudo mkdir -p lists/partial
sudo apt-get clean
sudo apt-get update
&lt;/code>&lt;/pre>&lt;p>他们认为原因是 apt 系统出现临时性的缓存故障，所以用 clean 的方式来清除。
我不知道在当时是否真的有效，毕竟那篇博客写在 2012 年，当时 Debian 6.0 也才发布一年，决不至于密钥出现过期，怀疑到缓存身上也
情有可原。&lt;/p>
&lt;p>但是当前我的密钥是真的过期了，invalid since 923d(ays)，我尝试了这种 clean 的方法，并不能生效。看来解决方案也有保持期啊。&lt;/p>
&lt;h2 id="密钥续期">密钥续期？&lt;/h2>
&lt;p>另一种方案是续期密钥，&lt;a href="https://askubuntu.com/a/837004">比如这里&lt;/a>&lt;/p>
&lt;pre>&lt;code>sudo apt-key list | grep &amp;quot;expired: &amp;quot;
sudo apt-key adv --keyserver keys.gnupg.net --recv-keys [KEY]
&lt;/code>&lt;/pre>&lt;p>这种方案想为过期的密钥续期，使其可用。
我先在系统里查看了密钥的情况，确实过期的太久。&lt;/p>
&lt;pre>&lt;code>$ sudo apt-key list
/etc/apt/trusted.gpg
--------------------
..........................
..........................
..........................
pub 2048R/6D849617 2009-01-24 [expired: 2013-01-23]
uid Debian-Volatile Archive Automatic Signing Key (5.0/lenny)
pub 4096R/B98321F9 2010-08-07 [expired: 2014-08-05]
uid Squeeze Stable Release Key &amp;lt;debian-release@lists.debian.org&amp;gt;
..........................
..........................
..........................
&lt;/code>&lt;/pre>&lt;p>于是用 &lt;code>apt-key adv&lt;/code> 进行续期，再次查看密钥情况，发现并没有多少改观，
时间确实前进更新了几个月，但是依旧保持着 expired 状态，毕竟还差很多年。&lt;/p>
&lt;p>我想可能和第一种方案是一样的问题，“方案过期”。在当时版本还支持的时候，应该是有效果的，但是当前情况已经
发生了变化，错误信息是一样的，但是问题本身已经出现了本质的变化。&lt;/p>
&lt;h1 id="当前的正确方案">当前的正确方案&lt;/h1>
&lt;p>于是继续搜索，在&lt;a href="https://wiki.debian.org/DebianSqueeze">官方文档的FAQ部分&lt;/a>，找到了官方解答。&lt;/p>
&lt;p>1.修改 source.list&lt;/p>
&lt;pre>&lt;code>deb http://archive.debian.org/debian squeeze main
deb http://archive.debian.org/debian squeeze-lts main
&lt;/code>&lt;/pre>&lt;p>2.同时配置 apt，添加以下内容到 /etc/apt/apt.conf(没有则新建)&lt;/p>
&lt;pre>&lt;code>Acquire::Check-Valid-Until false;
&lt;/code>&lt;/pre>&lt;p>其中特别提到了 &lt;code>The second line will fail with an &amp;quot;expired&amp;quot; type message, so you also need to ...&lt;/code>，so sweet！&lt;/p>
&lt;p>既然 Debian Squeeze 已经不再更新，apt source 也划入了 archive，这个方案应该是最“长治久安”的。&lt;/p>- https://dreamanddead.github.io/post/configure-debian-squeeze-apt-source/ - Attribution-NonCommercial-ShareAlike 4.0 International</description></item><item><title>How uri match nginx location</title><link>https://dreamanddead.github.io/post/nginx-location/</link><pubDate>Mon, 28 May 2018 09:10:00 +0000</pubDate><guid>https://dreamanddead.github.io/post/nginx-location/</guid><description>Brain=f(X)? https://dreamanddead.github.io/post/nginx-location/ -&lt;p>location 是 nginx 配置中出现最频繁的配置项，一个 uri 是如何与多个 location 进行匹配的？
在有多个 location 都匹配的情况下，如何决定使用哪一个 location 作为匹配结果？&lt;/p>
&lt;p>清晰内部机制之后，疑问自然迎刃而解。&lt;/p>
&lt;h1 id="location-规则类型">location 规则类型&lt;/h1>
&lt;p>根据&lt;a href="http://nginx.org/en/docs/http/ngx_http_core_module.html#location">官方文档&lt;/a>，location 配置语法如下&lt;/p>
&lt;pre>&lt;code>Syntax: location [ = | ~ | ~* | ^~ ] uri { ... }
location @name { ... }
Default: —
Context: server, location
&lt;/code>&lt;/pre>&lt;p>其语法对应了 location 规则的 5 种类型：&lt;/p>
&lt;h2 id="prefix">prefix&lt;/h2>
&lt;p>前缀匹配规则，如果 uri 包含相应的前缀，则匹配成功。&lt;/p>
&lt;p>语法上， location 之后单纯跟上 uri，没有任何操作符。&lt;/p>
&lt;pre>&lt;code>location /api/v1 {
....
}
&lt;/code>&lt;/pre>&lt;h2 id="exact-uri">exact uri&lt;/h2>
&lt;p>完全匹配规则，如果 uri 和规则完全相同，则匹配成功。&lt;/p>
&lt;p>语法上，使用 = 操作符。&lt;/p>
&lt;pre>&lt;code>location = /api/v1 {
....
}
&lt;/code>&lt;/pre>&lt;h2 id="case-sensitive-regex">case-sensitive regex&lt;/h2>
&lt;p>区分大小写的正则表达式匹配规则，如果 uri 与正则表达式相匹配，则匹配成功。
正则表达式使用 &lt;a href="https://www.pcre.org/">pcre library&lt;/a>，语法和 perl 兼容。&lt;/p>
&lt;p>语法上，使用 ~ 操作符。&lt;/p>
&lt;p>这里有一个细节，uri 作为一个 string，只要 regex 匹配其中的一部分，即可算作匹配。
比如有如下的规则，&lt;/p>
&lt;pre>&lt;code>location ~ /api/v1 {
....
}
&lt;/code>&lt;/pre>&lt;p>可以匹配 uri&lt;/p>
&lt;ul>
&lt;li>/api/v1/login&lt;/li>
&lt;li>/pod/api/v1&lt;/li>
&lt;/ul>
&lt;p>如果想要匹配整个 string，则要使用 &lt;code>^ $&lt;/code>。
比如，&lt;/p>
&lt;pre>&lt;code>location ~ ^/api/v1.*$ {
....
}
&lt;/code>&lt;/pre>&lt;p>可以匹配 uri&lt;/p>
&lt;ul>
&lt;li>/api/v1/login&lt;/li>
&lt;/ul>
&lt;p>不可以匹配&lt;/p>
&lt;ul>
&lt;li>/pod/api/v1&lt;/li>
&lt;/ul>
&lt;h2 id="case-insensitive-regex">case-insensitive regex&lt;/h2>
&lt;p>规则同上，区别只在于，正则表达式不区分大小写。
&lt;strong>不&lt;/strong>区分大小写的正则表达式匹配规则，如果 uri 与正则表达式相匹配，则匹配成功。&lt;/p>
&lt;p>语法上，使用 &lt;code>~*&lt;/code> 操作符。&lt;/p>
&lt;pre>&lt;code>location ~* /api/v1 {
....
}
&lt;/code>&lt;/pre>&lt;h2 id="disable-regex-prefix">disable regex prefix&lt;/h2>
&lt;p>匹配流程与 prefix 规则相同，有一点区别在于，如果最长匹配是当前规则，则之后不进行 正则表达式 规则的搜索。
这一点可能有些难以理解，后面会详细讲解。&lt;/p>
&lt;p>语法上，使用 &lt;code>^~&lt;/code> 操作符。&lt;/p>
&lt;pre>&lt;code>location ^~ /api/v1 {
....
}
&lt;/code>&lt;/pre>&lt;h1 id="uri-如何选择-location">uri 如何选择 location&lt;/h1>
&lt;p>前面讲到，location 规则有 5 种类型，那么 uri 如何在多种不同类型的 location 规则之间，
最终选择到唯一的 location 规则呢？&lt;/p>
&lt;p>根据&lt;a href="http://nginx.org/en/docs/http/ngx_http_core_module.html#location">官方文档&lt;/a>的描述，详细规则如下：&lt;/p>
&lt;ol>
&lt;li>如果存在 exact uri 规则与 uri 匹配，至步骤 6&lt;/li>
&lt;li>在所有 prefix 规则和 disable regex prefix 规则中进行匹配（&lt;strong>与这些规则定义的顺序无关&lt;/strong>），
如果没有匹配到规则，至步骤 3；如果存在匹配的规则，选择出最长匹配 uri 的规则：
&lt;ul>
&lt;li>如果规则是 disable regex prefix 类型，至步骤 6&lt;/li>
&lt;li>如果规则是 prefix 类型，记住当前匹配的 prefix 规则，选为待定，至步骤 3&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>逐个遍历 case-sensitive regex 规则和 case-insensitive regex 规则（&lt;strong>按照这些规则定义的前后顺序&lt;/strong>）:
&lt;ul>
&lt;li>如果规则匹配，则遍历终止，至步骤 6&lt;/li>
&lt;li>如果规则没有匹配，则继续&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>如果之前有 prefix 规则条目被选择为待定，至步骤 6&lt;/li>
&lt;li>匹配失败，返回404，结束&lt;/li>
&lt;li>选择当前规则，使用其配置，结束&lt;/li>
&lt;/ol>
&lt;p>&lt;img src="http://on7blnbb0.bkt.clouddn.com/18-9-28/21577003.jpg" alt="">&lt;/p>
&lt;h1 id="faq">FAQ&lt;/h1>
&lt;p>有人可能会困惑，如果 prefix 与 disable regex prefix 规则相同，比如，&lt;/p>
&lt;pre>&lt;code>location /static/ {
....
}
location ^~ /static/ {
....
}
&lt;/code>&lt;/pre>&lt;p>最终会匹配哪一条规则？是否应该继续查找 regex 规则？&lt;/p>
&lt;p>实际上，这种情况 nginx 会报错，&lt;code>nginx: [emerg] duplicate location &amp;quot;/static/&amp;quot; in /etc/nginx/conf.d/location.conf&lt;/code>，
所以这种情况是不用考虑的。&lt;/p>
&lt;h1 id="示例">示例&lt;/h1>
&lt;p>示例项目在 &lt;a href="https://github.com/DreamAndDead/nginx-example">nginx example&lt;/a>，
参考 readme 文件，在本地启动 nginx server。&lt;/p>
&lt;p>其中对 nginx 的配置如下，使用 curl 进行检测。&lt;/p>
&lt;script src="https://gist-it.appspot.com/github/DreamAndDead/nginx-example/blob/master/location/conf.d/location.conf?footer=minimal&amp;slice=0:34">&lt;/script>
&lt;p>exact uri 规则先于所有规则（与规则定义的顺序无关），所以这里匹配了 &lt;code>= /&lt;/code> exact uri 规则而不是 &lt;code>/&lt;/code> prefix 规则。&lt;/p>
&lt;pre>&lt;code>$ curl http://localhost:8080/
uri &amp;quot;/&amp;quot;: exact match &amp;quot;location = /&amp;quot;
&lt;/code>&lt;/pre>&lt;p>exact uri 规则先于所有规则（与规则定义的顺序无关），所以这里没有匹配 &lt;code>^~ /static/&lt;/code> disable regex prefix 和 &lt;code>~* \.PNG$&lt;/code>，&lt;code>~ \.png$&lt;/code> regex 规则。&lt;/p>
&lt;p>正是因为 exact uri 规则的这种属性，如果有高频率使用的 uri，建议使用 exact uri 匹配，加快匹配速度。&lt;/p>
&lt;pre>&lt;code>$ curl http://localhost:8080/static/logo.png
uri &amp;quot;/static/logo.png&amp;quot;: exact uri match &amp;quot;location = /static/logo.png&amp;quot;
&lt;/code>&lt;/pre>&lt;p>prefix 规则中，匹配 uri 长度最长的规则会选中，所以没有选中 &lt;code>/&lt;/code> prefix 规则。同样的，从这一点来看，&lt;code>/api&lt;/code> 和 &lt;code>/api/&lt;/code> 是不一样的。&lt;/p>
&lt;pre>&lt;code>$ curl http://localhost:8080/api
uri &amp;quot;/api&amp;quot;: prefix match &amp;quot;location /api&amp;quot;
$ curl http://localhost:8080/api/
uri &amp;quot;/api/&amp;quot;: prefix match &amp;quot;location /api/&amp;quot;
$ curl http://localhost:8080/api/v1
uri &amp;quot;/api/v1&amp;quot;: prefix match &amp;quot;location /api/&amp;quot;
&lt;/code>&lt;/pre>&lt;p>如果匹配了 &lt;code>^~ /static/&lt;/code> disable regex prefix 规则，则不再去搜索之后的 regex 规则。&lt;/p>
&lt;pre>&lt;code>$ curl http://localhost:8080/static/thinkpad.png
uri &amp;quot;/static/thinkpad.png&amp;quot;: disable regex prefix match &amp;quot;location ^~ /static/&amp;quot;
&lt;/code>&lt;/pre>&lt;p>在 regex 规则的匹配中，定义的先后顺序是重要的，所以 &lt;code>~ \.png$&lt;/code> case-sensitive 规则永远不会被匹配。&lt;/p>
&lt;pre>&lt;code>$ curl http://localhost:8080/files/large.png
uri &amp;quot;/files/large.png&amp;quot;: case-insesitive regex match &amp;quot;location ~* \.PNG(dollar)&amp;quot;
$ curl http://localhost:8080/files/large.PNG
uri &amp;quot;/files/large.PNG&amp;quot;: case-insesitive regex match &amp;quot;location ~* \.PNG(dollar)&amp;quot;
&lt;/code>&lt;/pre>&lt;p>即使先匹配了 &lt;code>/api/&lt;/code> prefix 规则作为待定，但是也匹配了 regex 规则，优先使用 regex 规则。&lt;/p>
&lt;pre>&lt;code>$ curl http://localhost:8080/api/v1/file/logo.png
uri &amp;quot;/api/v1/file/logo.png&amp;quot;: case-insesitive regex match &amp;quot;location ~* \.PNG(dollar)&amp;quot;
&lt;/code>&lt;/pre>&lt;p>存在 &lt;code>/&lt;/code> prefix 规则是有益的，收容所有未明确定位的 uri，再做错误处理。&lt;/p>
&lt;pre>&lt;code>$ curl http://localhost:8080/no-where
uri &amp;quot;/no-where&amp;quot;: prefix match &amp;quot;location /&amp;quot;
&lt;/code>&lt;/pre>- https://dreamanddead.github.io/post/nginx-location/ - Attribution-NonCommercial-ShareAlike 4.0 International</description></item><item><title>记一次 Jmeter 分布式测试故障调试过程</title><link>https://dreamanddead.github.io/post/jmeter-distributed-testing-trouble-shooting/</link><pubDate>Fri, 25 May 2018 02:10:00 +0000</pubDate><guid>https://dreamanddead.github.io/post/jmeter-distributed-testing-trouble-shooting/</guid><description>Brain=f(X)? https://dreamanddead.github.io/post/jmeter-distributed-testing-trouble-shooting/ -&lt;p>之前总结了 jmeter 分布式测试的过程，在部署过程中提到，要在 system.properties 中配置自己的 IP。&lt;/p>
&lt;p>至于为什么要这么做，源于这一次 debug 的过程。&lt;/p>
&lt;h1 id="运行环境">运行环境&lt;/h1>
&lt;p>mint, ubuntu 两台主机作为 master 节点，一台 win server 作为 slave 节点，采用分布式的方式，
对 target 进行测试。&lt;/p>
&lt;h1 id="问题">问题&lt;/h1>
&lt;p>问题很奇怪，以 mint 系统作为 master，调度分布式测试没有问题，&lt;/p>
&lt;pre>&lt;code>$ jmeter -n -t ./test.jmx -R 10.27.2.210 -l test.jtl -e -o report_test
Creating summariser &amp;lt;summary&amp;gt;
Created the tree successfully using ./888.jmx
Configuring remote engine: 10.27.2.210
Starting remote engines
Starting the test @ Wed May 16 10:09:24 CST 2018 (1526436564379)
Remote engines have been started
Waiting for possible Shutdown/StopTestNow/Heapdump message on port 4445
summary = 1 in 00:00:00 = 3.5/s Avg: 20 Min: 20 Max: 20 Err: 0 (0.00%)
Tidying up remote @ Wed May 16 10:09:26 CST 2018 (1526436566910)
... end of run
&lt;/code>&lt;/pre>&lt;p>而使用另一台 ubuntu 主机作为 master，调度测试，则卡在 waiting 这一行，&lt;/p>
&lt;pre>&lt;code>$ jmeter -n -t ./test.jmx -R 10.27.2.210 -l test.jtl -e -o report_test
Creating summariser &amp;lt;summary&amp;gt;
Created the tree successfully using ./888.jmx
Configuring remote engine: 10.27.2.210
Starting remote engines
Starting the test @ Wed May 16 10:09:24 CST 2018 (1526436564379)
Remote engines have been started
Waiting for possible Shutdown/StopTestNow/Heapdump message on port 4445
&lt;/code>&lt;/pre>&lt;h2 id="尝试1防火墙干扰">尝试1：防火墙干扰&lt;/h2>
&lt;p>第一种猜测，应该是 ubuntu 系统的防火墙干扰，阻止了 4445 端口的数据。&lt;/p>
&lt;p>先关闭防火墙&lt;/p>
&lt;pre>&lt;code>$ sudo service ufw stop
&lt;/code>&lt;/pre>&lt;p>再次运行测试，依旧卡在 waiting 那一行，看来问题没有那么简单。&lt;/p>
&lt;h2 id="尝试2关闭监听功能">尝试2：关闭监听功能&lt;/h2>
&lt;p>以 &lt;code>Waiting for possible Shutdown/StopTestNow/Heapdump message on port 4445&lt;/code>
信息在网上查找，有人建议修改 jmeter 设置，不监听 shutdown 信息，相应的也就不会开启 4445 端口，
理论上就不会卡在 waiting 那里，而是直接跳过。&lt;/p>
&lt;p>在 ubuntu 主机，修改 jmeter.properties 文件，&lt;/p>
&lt;script src="https://gist-it.appspot.com/github/apache/jmeter/blob/v4_0/bin/jmeter.properties?footer=minimal&amp;slice=1074:1076">&lt;/script>
&lt;p>修改为&lt;/p>
&lt;pre>&lt;code>jmeterengine.nongui.maxport=0
&lt;/code>&lt;/pre>&lt;p>但是依旧不能工作。&lt;/p>
&lt;h2 id="尝试3端口没有开启">尝试3：端口没有开启&lt;/h2>
&lt;p>在 mint 作为 master，成功调度分布式测试的过程中，mint 主机自身打开了 4445 udp 端口，
用 netstat 可以查看的到。&lt;/p>
&lt;p>但是，在 ubuntu 调度的时候，用 netstat 也可以查看到 4445 udp 端口已经打开。
而且用 &lt;a href="https://unix.stackexchange.com/a/266368/278131">netcat 测试 udp 端口&lt;/a>也是可用的。&lt;/p>
&lt;pre>&lt;code>$ nc -vz -u 10.27.2.13 4445
Connection to 10.27.2.13 4445 port [udp/*] succeeded!
&lt;/code>&lt;/pre>&lt;p>看来也不是端口未开放的问题。&lt;/p>
&lt;h2 id="尝试4信息被阻塞">尝试4：信息被阻塞&lt;/h2>
&lt;p>猜测是不是在哪个环节，shutdown message 被阻拦，导致收不过消息，而一直卡在那里。&lt;/p>
&lt;p>于是在 mint 主机运行测试的时候，用 wireshark 监听所有发送与 4445 udp 的信息，然而！什么都没有！&lt;/p>
&lt;p>这样看来，之前对问题的判断是不准确的。一直以为和 shutdown message 有关，其实正常运行一次测试，根本不必要有 shutdown message。&lt;/p>
&lt;h2 id="尝试5在日志中寻找">尝试5：在日志中寻找&lt;/h2>
&lt;p>之前一直被表面现象迷惑，认为是 master 的问题，直到进入 slave 节点查看日志 &lt;code>jmeter-server.log&lt;/code>。
发现这样的错误输出，&lt;/p>
&lt;pre>&lt;code>2018-05-14 18:29:56,240 ERROR o.a.j.s.BatchSampleSender: testEnded(host)
java.rmi.ConnectException: Connection refused to host: 127.0.1.1; nested exception is:
java.net.ConnectException: Connection refused: connect
at sun.rmi.transport.tcp.TCPEndpoint.newSocket(Unknown Source) ~[?:1.8.0_162]
at sun.rmi.transport.tcp.TCPChannel.createConnection(Unknown Source) ~[?:1.8.0_162]
at sun.rmi.transport.tcp.TCPChannel.newConnection(Unknown Source) ~[?:1.8.0_162]
at sun.rmi.server.UnicastRef.invoke(Unknown Source) ~[?:1.8.0_162]
at java.rmi.server.RemoteObjectInvocationHandler.invokeRemoteMethod(Unknown Source) ~[?:1.8.0_162]
at java.rmi.server.RemoteObjectInvocationHandler.invoke(Unknown Source) ~[?:1.8.0_162]
at com.sun.proxy.$Proxy20.testEnded(Unknown Source) ~[?:?]
at org.apache.jmeter.samplers.BatchSampleSender.testEnded(BatchSampleSender.java:127) [ApacheJMeter_core.jar:4.0 r1823414]
at org.apache.jmeter.samplers.DataStrippingSampleSender.testEnded(DataStrippingSampleSender.java:86) [ApacheJMeter_core.jar:4.0 r1823414]
at org.apache.jmeter.samplers.RemoteListenerWrapper.testEnded(RemoteListenerWrapper.java:90) [ApacheJMeter_core.jar:4.0 r1823414]
at org.apache.jmeter.engine.StandardJMeterEngine.notifyTestListenersOfEnd(StandardJMeterEngine.java:229) [ApacheJMeter_core.jar:4.0 r1823414]
at org.apache.jmeter.engine.StandardJMeterEngine.run(StandardJMeterEngine.java:495) [ApacheJMeter_core.jar:4.0 r1823414]
at java.lang.Thread.run(Unknown Source) [?:1.8.0_162]
Caused by: java.net.ConnectException: Connection refused: connect
at java.net.DualStackPlainSocketImpl.connect0(Native Method) ~[?:1.8.0_162]
at java.net.DualStackPlainSocketImpl.socketConnect(Unknown Source) ~[?:1.8.0_162]
at java.net.AbstractPlainSocketImpl.doConnect(Unknown Source) ~[?:1.8.0_162]
at java.net.AbstractPlainSocketImpl.connectToAddress(Unknown Source) ~[?:1.8.0_162]
at java.net.AbstractPlainSocketImpl.connect(Unknown Source) ~[?:1.8.0_162]
at java.net.PlainSocketImpl.connect(Unknown Source) ~[?:1.8.0_162]
at java.net.SocksSocketImpl.connect(Unknown Source) ~[?:1.8.0_162]
at java.net.Socket.connect(Unknown Source) ~[?:1.8.0_162]
at java.net.Socket.connect(Unknown Source) ~[?:1.8.0_162]
at java.net.Socket.&amp;lt;init&amp;gt;(Unknown Source) ~[?:1.8.0_162]
at java.net.Socket.&amp;lt;init&amp;gt;(Unknown Source) ~[?:1.8.0_162]
at sun.rmi.transport.proxy.RMIDirectSocketFactory.createSocket(Unknown Source) ~[?:1.8.0_162]
at sun.rmi.transport.proxy.RMIMasterSocketFactory.createSocket(Unknown Source) ~[?:1.8.0_162]
... 13 more
&lt;/code>&lt;/pre>&lt;p>为什么地址是 127.0.1.1 ？为什么要向这个地址通信？&lt;/p>
&lt;p>根据对 jmeter master/slave 通信机制的理解，不仅 master 主动向 slave 通信， slave 也向 master 主动通信，自然要知道彼此的地址。&lt;/p>
&lt;p>由此联想到，在 slave 节点多网卡的时候，一般都&lt;a href="https://github.com/apache/jmeter/blob/v4_0/bin/jmeter-server#L30">设置 hostname&lt;/a>，猜测 master 也需要进行同样的设置，告知 slave 自己的通信地址。&lt;/p>
&lt;p>于是在 ubuntu master 主机的 &lt;a href="https://github.com/apache/jmeter/blob/v4_0/bin/system.properties">system.properties&lt;/a> 最后一行添加，&lt;/p>
&lt;pre>&lt;code>java.rmi.server.hostname=10.27.2.13
&lt;/code>&lt;/pre>&lt;p>测试终于正常运行了。&lt;/p>
&lt;h1 id="写在最后">写在最后&lt;/h1>
&lt;p>推而广之，在 jmeter 的所有节点，无论 master/slave，都显示设置自己的 IP 地址。&lt;/p>
&lt;p>&lt;a href="https://github.com/apache/jmeter/blob/v4_0/bin/system.properties">system.properties&lt;/a> 最后一行添加&lt;/p>
&lt;pre>&lt;code>java.rmi.server.hostname=&amp;lt;IP addr&amp;gt;
&lt;/code>&lt;/pre>- https://dreamanddead.github.io/post/jmeter-distributed-testing-trouble-shooting/ - Attribution-NonCommercial-ShareAlike 4.0 International</description></item><item><title>使用Jmeter进行分布式测试</title><link>https://dreamanddead.github.io/post/jmeter-distributed-testing/</link><pubDate>Thu, 24 May 2018 19:10:00 +0000</pubDate><guid>https://dreamanddead.github.io/post/jmeter-distributed-testing/</guid><description>Brain=f(X)? https://dreamanddead.github.io/post/jmeter-distributed-testing/ -&lt;p>业务发展初期，关注的更多是功能，直至使用量增加到一定量级，就要考虑高并发，程序性能等问题，
因此性能测试就成为重要的一环。&lt;/p>
&lt;p>Loadrunner 是性能测试工具领域的王者，但是闭源无法自由（free）使用。相较之下，开源的 Jmeter 算是一个
不错的选择。&lt;/p>
&lt;p>性能测试要达到一定的量级，仅仅靠单机是不够的，必须协调多方资源，模拟高数量级的并发。Jmeter 提供的
分布式测试方案刚好满足这一点，这也是本文的由来。&lt;/p>
&lt;h1 id="install">Install&lt;/h1>
&lt;p>Jmeter 使用 Java 编写，要求本机必须存在 Java 环境，推荐使用 8 版本。&lt;/p>
&lt;pre>&lt;code>$ sudo apt install openjdk-8-jdk
&lt;/code>&lt;/pre>&lt;p>官方推荐使用 Jmeter 的最新版本，新版本会修复旧有的 bug 并且有新的 feature。
目前最新版本是 4.0，&lt;a href="https://jmeter.apache.org/download_jmeter.cgi">直接下载 Binaries 压缩包&lt;/a>即可。&lt;/p>
&lt;p>解压之后，目录结构如下&lt;/p>
&lt;pre>&lt;code>apache-jmeter-4.0
├── backups
├── bin
├── docs
├── extras
├── lib
├── LICENSE
├── licenses
├── NOTICE
├── printable_docs
└── README.md
&lt;/code>&lt;/pre>&lt;p>主要程序都在 bin/ 目录下，在这里我们主要使用&lt;/p>
&lt;pre>&lt;code>./bin/jmeter
./bin/jmeter-server
&lt;/code>&lt;/pre>&lt;h1 id="gui-or-not">GUI or not&lt;/h1>
&lt;p>Jmeter 运行有两种模式，GUI和非GUI。&lt;/p>
&lt;p>GUI由于可视化，方便构建 jmx（Jmeter测试计划文件，本质是 xml）。
而在运行大量测试的时候，GUI会成为资源的负担。
官方推荐使用 GUI 模式来构建测试计划，并进行很小的并发量来调试测试计划，
使用非 GUI 模式来运行大并发量的测试计划。&lt;/p>
&lt;h1 id="distributed">Distributed&lt;/h1>
&lt;p>Jmeter 可以从单机进行测试，但是毕竟单机的资源有限，所发起的并发自然是有限的，
这里我们主要讨论分布式测试。&lt;/p>
&lt;p>分布式协同多个主机，同时发起测试，并发量随着主机的增加而增加。
由 Master 发起测试，并将 jmx 传输到所有的 Slave，每个 Slave 都会执行 jmx（Slave 个数决定并发量的翻倍数），
并将相关数据反馈回 Master，由 Master 整合数据，呈现报表。&lt;/p>
&lt;figure>
&lt;img src="images/jmeter-distributed-testing-structure.png"
alt="Jmeter分布式测试模型"/> &lt;figcaption>
&lt;p>Jmeter分布式测试模型&lt;/p>
&lt;/figcaption>
&lt;/figure>
&lt;p>从程序的角度来看，&lt;code>./bin/jmeter&lt;/code>于 Master 运行，发起测试。
&lt;code>./bin/jmeter-server&lt;/code>于 Slave 运行，从 Master 获取 jmx，并对目标发起测试。&lt;/p>
&lt;p>jmeter client 运行于 Master 节点，jmeter server 运行于 Slave 节点，这点概念有些反直觉，我在和同事
交流的时候就经常搞混。所以一开始就要明确概念，推荐 Master/Slave 概念，清晰且符合直觉。&lt;/p>
&lt;h2 id="deploy">Deploy&lt;/h2>
&lt;p>部署 Jmeter 分布式并不复杂，关键要了解怎么样的测试效率是最高的。&lt;/p>
&lt;ul>
&lt;li>Master/Slave/Target 最好在一个内网内&lt;/li>
&lt;li>如果 Target 在外网，就要保证网络出口的带宽要足够&lt;/li>
&lt;/ul>
&lt;p>进行测试前，所有的 Master/Slave 节点都要已经安装了 Jmeter，参考 &lt;a href="#install">Install&lt;/a> 一节。
所有的 Slave 运行 &lt;code>./bin/jmeter-server&lt;/code>，监听来自 Master 发起的测试。&lt;/p>
&lt;p>在运行 &lt;code>jmeter-server&lt;/code> 之前，部分配置可能是必要的。&lt;/p>
&lt;p>&lt;strong>关闭rmi ssl&lt;/strong>&lt;/p>
&lt;p>Jmeter默认将 rmi ssl 打开，在 Master/Slave 的通信中使用安全传输，如果都在内网的话，就不必配置 ssl 证书了，
提高效率并节省时间。&lt;/p>
&lt;p>在 Master/Slave 节点的 &lt;code>./bin/jmeter.properties&lt;/code> 文件中，&lt;a href="https://github.com/apache/jmeter/blob/trunk/bin/jmeter.properties#L330">修改相应配置&lt;/a>&lt;/p>
&lt;pre>&lt;code># 修改server.rmi.ssl.disable=false为
server.rmi.ssl.disable=true
&lt;/code>&lt;/pre>&lt;p>&lt;strong>关闭防火墙&lt;/strong>&lt;/p>
&lt;p>关闭 Master/Slave 节点上的防火墙，防止对通信中的干扰。&lt;/p>
&lt;pre>&lt;code>$ sudo service ufw stop
&lt;/code>&lt;/pre>&lt;p>&lt;strong>设置IP&lt;/strong>&lt;/p>
&lt;p>在 Master/Slave 节点的 &lt;code>./bin/system.properties&lt;/code> 中，最后一行添加&lt;/p>
&lt;pre>&lt;code>java.rmi.server.hostname=&amp;lt;当前节点的IP&amp;gt;
&lt;/code>&lt;/pre>&lt;p>为什么这么做，具体参考 &lt;a href="https://dreamanddead.github.io/2018/05/25/jmeter-distributed-testing-trouble-shooting.html">jmeter-distributed-testing-trouble-shooting&lt;/a>，实力踩坑。&lt;/p>
&lt;h1 id="run">Run&lt;/h1>
&lt;p>在 Master 节点启动测试前，先&lt;a href="https://github.com/apache/jmeter/blob/trunk/bin/jmeter.properties#L254">将 Slave 列表加入 Master 配置列表&lt;/a>中。
比如&lt;/p>
&lt;pre>&lt;code>remote_hosts=10.28.2.2, 10.28.2.200, 10.28.3.9
&lt;/code>&lt;/pre>&lt;p>由非 GUI 模式发起测试&lt;/p>
&lt;pre>&lt;code>$ rm -rf test.jtl report_test
$ jmeter -n -t ./test.jmx -r -l test.jtl -e -o report_test
&lt;/code>&lt;/pre>&lt;ul>
&lt;li>&lt;code>-n&lt;/code> means no GUI&lt;/li>
&lt;li>&lt;code>-t&lt;/code> 指定 jmx&lt;/li>
&lt;li>&lt;code>-r&lt;/code> 将使用配置文件中 remote_hosts 作为 Slave 节点&lt;/li>
&lt;li>&lt;code>-l&lt;/code> 指定日志文件&lt;/li>
&lt;li>&lt;code>-e&lt;/code> 测试结束后生成统计报表&lt;/li>
&lt;li>&lt;code>-o&lt;/code> 指定数据报表路径。将所有请求的响应作了统计分析，生成网页，非常详细&lt;/li>
&lt;/ul>
&lt;h1 id="metric">Metric&lt;/h1>
&lt;p>上述测试，只能在测试结束之后，分析响应时间的分布，错误率之类的信息，并不能得到任何
实时性的数据。&lt;/p>
&lt;p>实时性的数据是有用的，可以从两个角度来分类&lt;/p>
&lt;ul>
&lt;li>一类是jmeter的数据，如当前线程数，当前发送请求数，当前响应时间分布等&lt;/li>
&lt;li>一类是被测试主机的性能数据，如cpu使用率，内存使用量等&lt;/li>
&lt;/ul>
&lt;p>这样可以将相同时间的数据建立关联关系，有助于找出被测试目标的性能瓶颈可能出现在哪里。&lt;/p>
&lt;p>在实践过程中，推荐使用 influxdb + grafana 的结合，因为&lt;/p>
&lt;ul>
&lt;li>多数应用的 metric 都可以用 influxdb 来收集（包括 jmeter cadvisor gitlab 等）&lt;/li>
&lt;li>grafana 前端优雅好用，完美支持 influxdb 作为后端&lt;/li>
&lt;/ul>
&lt;p>Jmeter 本身的 metric 可以用自身的 Backend Listener 来产生，并传输至 influxdb 中。
Jmeter 有两种 Backend Listener&lt;/p>
&lt;ul>
&lt;li>Graphite&lt;/li>
&lt;li>Influxdb&lt;/li>
&lt;/ul>
&lt;p>本质上来讲，两种 Listener 产生的数据信息量都是一样的，不过对于数据的组织形式有所不同。
Graphite本身是一个自带前后端的数据可视化工具，但是自己觉得不是特别好用，好在 Influxdb
内部提供对 Graphite 的数据适配，可以采集 Graphite 格式的数据，:thumbsup:。&lt;/p>
&lt;h1 id="example">Example&lt;/h1>
&lt;p>在这个示例中，将详细描述上述工具是如何协作的。示例项目在 &lt;a href="https://github.com/DreamAndDead/jmeter-distributed-testing-example">https://github.com/DreamAndDead/jmeter-distributed-testing-example&lt;/a>&lt;/p>
&lt;h2 id="plan">Plan&lt;/h2>
&lt;p>测试针对的目标是 gitlab ，gitlab 提供了丰富的 RESTful 接口供资源操作，通过 Jmeter 分布式，
对部分接口进行压力测试。&lt;/p>
&lt;figure>
&lt;img src="images/jmeter-distributed-test.png"
alt="example test architecture"/> &lt;figcaption>
&lt;p>example test architecture&lt;/p>
&lt;/figcaption>
&lt;/figure>
&lt;ul>
&lt;li>Master 编辑测试计划，发起分布式测试&lt;/li>
&lt;li>Slave 对 gitlab 发起测试，同时将实时数据传送至 influxdb&lt;/li>
&lt;li>gitlab 以 docker 运行，提供 api 供 Slave 测试，同时将内部 metric 发送予 influxdb&lt;/li>
&lt;li>cadvisor 从 gitlab docker 中采集数据，发送至 influxdb&lt;/li>
&lt;li>influxdb 收集所有 metric 数据&lt;/li>
&lt;li>grafana 以 influxdb 作为后端，呈现所有 metric 数据&lt;/li>
&lt;/ul>
&lt;p>示例主要在于说明整个测试的结构，使用的测试计划非常简单，而且还有一些内网地址，可以根据需要，
做相应的调整。&lt;/p>
&lt;h3 id="master">Master&lt;/h3>
&lt;p>目前的结构是，Master/Slave/Target/Collector 都在一个内网内。这里 Master 并不需要特别的设置，
只需要确保已经安装配置了 Jmeter 并与 Slave 的连通性即可。&lt;/p>
&lt;h3 id="slave">Slave&lt;/h3>
&lt;p>同 Master，需要正确安装配置 Jmeter 并确保与 Master/Target/Collector 的连通性即可。&lt;/p>
&lt;h3 id="gitlab">gitlab&lt;/h3>
&lt;p>gitlab 占用资源比较多，用 docker 限定了资源上限，考察其在资源有限时的表现。&lt;/p>
&lt;p>具体启动参考&lt;a href="https://github.com/DreamAndDead/jmeter-distributed-testing-example/blob/master/setup/target">示例项目&lt;/a>。&lt;/p>
&lt;h3 id="cadvisor">cadvisor&lt;/h3>
&lt;p>与 gitlab docker 运行在同一主机，收集所有与 docker container 相关的数据。&lt;/p>
&lt;p>具体启动参考&lt;a href="https://github.com/DreamAndDead/jmeter-distributed-testing-example/blob/master/setup/target">示例项目&lt;/a>。&lt;/p>
&lt;h3 id="influxdb">influxdb&lt;/h3>
&lt;p>influxdb 在这里收集3方面数据，而且使用了3种数据采集的方式。&lt;/p>
&lt;p>数据的采集需要相关应用的支持，3方面的联通可参考&lt;a href="https://github.com/DreamAndDead/jmeter-distributed-testing-example/blob/master/setup/collector/readme.md">示例项目的说明&lt;/a>。&lt;/p>
&lt;h3 id="grafana">grafana&lt;/h3>
&lt;p>作为数据前端，从 influxdb 读取并呈现数据，重点在于如何取数据，这就要灵活判断相应的数据结构是什么。&lt;/p>
&lt;p>&lt;a href="https://github.com/DreamAndDead/jmeter-distributed-testing-example/blob/master/setup/collector/readme.md#config-grafana">参考示例项目文档&lt;/a>。&lt;/p>
&lt;h2 id="run-1">Run&lt;/h2>
&lt;p>在控制所有 Slave 的 Master，执行&lt;/p>
&lt;pre>&lt;code>$ ./bin/jmeter -n -t ./gitlab.jmx -r -l test.jtl -e -o report_test
&lt;/code>&lt;/pre>&lt;p>结束之后，在report_test的文件夹下，生成html报表，用浏览器访问查看报表。&lt;/p>
&lt;h1 id="pitfalls">Pitfalls&lt;/h1>
&lt;p>大部分情况下，分布式测试都运作的很流畅。近期同事发现了一个细节问题，自己明明联通了5台 Slave，却只在
grafana 看到了一台机器的并发量，其它的好像没有执行。&lt;/p>
&lt;p>到相应 Slave 节点，查看 &lt;code>jmeter-server.log&lt;/code> 文件，发现读取文件 csv 不存在。&lt;/p>
&lt;p>原来是因为测试计划中，设定从一个 csv 文件中，读取所有用户的信息，并循环执行创建操作，
但是 Master 在执行测试计划时，只分发 jmx 文件，Slave 由于找不到相应数据文件而失败。&lt;/p>
&lt;p>所以部署相关的数据文件需要我们自己想办法解决。并且！有时数据文件一模一样也不行，毕竟 Slave 执行的过程是相同的，
一样的文件必然导致其创建相同的用户，引发冲突，还要为每个 Slave 准备一份数据文件！:no_mouth:。&lt;/p>
&lt;h1 id="end">End&lt;/h1>
&lt;p>Jmeter 是一款很赞的软件，thanks apache。&lt;/p>- https://dreamanddead.github.io/post/jmeter-distributed-testing/ - Attribution-NonCommercial-ShareAlike 4.0 International</description></item><item><title>Deal with time in programming</title><link>https://dreamanddead.github.io/post/time-in-programming/</link><pubDate>Tue, 22 May 2018 08:08:00 +0000</pubDate><guid>https://dreamanddead.github.io/post/time-in-programming/</guid><description>Brain=f(X)? https://dreamanddead.github.io/post/time-in-programming/ -&lt;p>时间无处不在，在编程中也是如此。
比如程序日志，每一行必定包含时间。
通常不会对时间特别关注，除非日志时间不对，或者编程使用时间，取不到正确结果。&lt;/p>
&lt;p>其实在编程中只需明确几个简单的概念，就可以正确的使用时间。&lt;/p>
&lt;h1 id="unix-epoch-time">UNIX Epoch time&lt;/h1>
&lt;blockquote>
&lt;p>UNIX Epoch time是从1970年1月1日（UTC）到目前经过的秒数&lt;/p>
&lt;/blockquote>
&lt;p>从某种意义上讲，这个时间是“绝对的”，无论在地球上哪个国家，大家的 UNIX Epoch time 是一样的。&lt;/p>
&lt;p>它的值确定唯一，这个时间也在程序中广泛使用，比如用作机器间时间同步的 ntp 协议，同步的就是这个时间。
如果每一台机器上的 UNIX Epoch time 都相同，就是保持了时钟的一致。&lt;/p>
&lt;p>在 linux 上，可以通过 &lt;code>date&lt;/code> ，来查看时间&lt;/p>
&lt;pre>&lt;code>####### %s means seconds since 1970-01-01 00:00:00 UTC
####### %N means nanoseconds (000000000..999999999)
$ date +&amp;quot;%s.%N&amp;quot;
1526949060.804222827
&lt;/code>&lt;/pre>&lt;h1 id="timezone">Timezone&lt;/h1>
&lt;figure>
&lt;img src="images/timezone-map.png"
alt="时区图"/> &lt;figcaption>
&lt;p>时区图&lt;/p>
&lt;/figcaption>
&lt;/figure>
&lt;p>在同一时刻，经度距离较远不同地区，时钟的值是不一样的。比如北京时间的晚上8点，同时是纽约时间的早上7点。
那么 UNIX Epoch time 是如何确保不同地区的时间是一致的呢？&lt;/p>
&lt;p>原因在于时区，UNIX Epoch time 强制使用 UTC+0 时区的时间，而本地时间则使用各地区时区的时间。&lt;/p>
&lt;p>究竟什么是&lt;a href="http://www.timeofdate.com/articles/posts/timezone-introduction.html">时区&lt;/a>？&lt;/p>
&lt;blockquote>
&lt;p>时区是一个地区，在这个区域内，大家使用同样的标准时间&lt;/p>
&lt;/blockquote>
&lt;p>一个时区内的当地时间，用与世界标准时间 (UTC) 之间的偏移量来定义。这个偏移量可以表示为 UTC- 或 UTC+，后面接上偏移的小时数和分钟数。
因此时区这个名词常常被用来表示当地时间。比如国内大部分区域使用的就是 UTC+8 时间，在UTC时间的基础上，加8个小时，代表当地时间。&lt;/p>
&lt;p>为什么全球不使用一个时间，方便统一？&lt;/p>
&lt;p>其实时区的出现就是为了方便不同地区的人对时间概念认知。日出而作，日落而息是所有地区人们的生活习惯，很多地区根据当地的日出日落来设定时钟的时间。
而且你肯定不希望，原来你在家里早上8点起床，出差到另外一个地区之后，要变成晚上2点起床。毕竟晚上2点见到太阳
听起来并不怎么有趣。&lt;/p>
&lt;p>&lt;a href="http://www.timeofdate.com/articles/posts/timezone-history.html">时区发展的历史&lt;/a>有点复杂，很多细节，但是正因它的出现，
解决了各地地理差异而带来的时差问题，同时也让各地区的当地时间尽量贴合本地的太阳时间。&lt;/p>
&lt;h2 id="abbr">Abbr&lt;/h2>
&lt;p>用 UTC+8 这样的符号称谓当地时间毕竟不便，大部分地区都为各自地区的时区时间
起一个名称，代表自己的本地时间。
比如国内将本地时间称为China Standard Time，时区简称CST。&lt;/p>
&lt;p>按照这样的规律（人们的心理），同一时区会有很多个不同的时区简称，有时还会有重复冲突。
举例来说，在迈阿密，标准时间是 UTC-5，称作东部标准时间 (EST, Eastern Standard Time)。而在古巴的哈瓦那，同样的 UTC-5 标准时间，就被称为古巴标准时间 (CST, Cuba Standard Time)，而CST和国内的时区简称还是重复的！&lt;/p>
&lt;p>通常一个地区只关注自己的时间，不加时区简称，但是在世界范围内，如果有相应的重复冲突还是要额外注明。&lt;/p>
&lt;p>通常遇到最多的简称如，UTC，GMT，CST。&lt;/p>
&lt;ul>
&lt;li>UTC 代表&lt;a href="http://www.timeofdate.com/articles/posts/about-utc.html">世界标准时间&lt;/a>，Coordinated Universal Time&lt;/li>
&lt;li>GMT，时区简称，Greenwich Mean Time，UTC+0&lt;/li>
&lt;li>CST，时区简称（有重复冲突），China Standard Time，UTC+8&lt;/li>
&lt;/ul>
&lt;p>这里有 &lt;a href="https://www.timeanddate.com/time/zones/">时区简称的列表&lt;/a> 和 &lt;a href="https://www.timeanddate.com/time/map/">时区地图&lt;/a>，详细直观，值得一看。&lt;/p>
&lt;h2 id="in-linux">In linux&lt;/h2>
&lt;ul>
&lt;li>系统当前时区可以用 &lt;code>date&lt;/code> 查看&lt;/li>
&lt;li>时区地区由 &lt;code>/etc/timezone&lt;/code> 保存&lt;/li>
&lt;/ul>
&lt;pre>&lt;code>$ date +&amp;quot;%Z %z&amp;quot;
CST +0800
$ cat /etc/timezone
Asia/Shanghai
&lt;/code>&lt;/pre>&lt;ul>
&lt;li>默认 &lt;code>date&lt;/code> 使用本地时区呈现时间&lt;/li>
&lt;li>加上 &lt;code>-u&lt;/code>，使用 UTC 呈现时间&lt;/li>
&lt;/ul>
&lt;pre>&lt;code>$ date
2018年 04月 12日 星期四 16:03:16 CST
$ date -u
2018年 04月 12日 星期四 08:03:19 UTC
&lt;/code>&lt;/pre>&lt;p>保证系统的时区正确是很重要的！这个时区作为所有系统运行程序的默认时区，
如果这个时区不准确，在程序中就取不到准确的本地时间，程序的日志时间自然也有所偏差。&lt;/p>
&lt;p>在多数 linux 发行版中，可以通过修改 &lt;code>/etc/localtime&lt;/code> 软链接来设置时区。&lt;/p>
&lt;pre>&lt;code>$ sudo ln -sf /usr/share/zoneinfo/Africa/Nairobi /etc/localtime
&lt;/code>&lt;/pre>&lt;p>相比之下，使用 &lt;code>timedatectl&lt;/code> 比较稳妥。&lt;/p>
&lt;pre>&lt;code>$ timedatectl status
Local time: 一 2018-05-21 09:01:08 CST
Universal time: 一 2018-05-21 01:01:08 UTC
RTC time: 一 2018-05-21 01:01:08
Time zone: Asia/Shanghai (CST, +0800)
Network time on: yes
NTP synchronized: yes
RTC in local TZ: no
$ timedatectl set-timezone Asia/Hong_Kong
$ timedatectl status
Local time: 一 2018-05-21 09:01:47 HKT
Universal time: 一 2018-05-21 01:01:47 UTC
RTC time: 一 2018-05-21 01:01:47
Time zone: Asia/Hong_Kong (HKT, +0800)
Network time on: yes
NTP synchronized: yes
RTC in local TZ: no
&lt;/code>&lt;/pre>&lt;p>参考： &lt;a href="https://www.tecmint.com/check-linux-timezone/">如何设置linux timezone&lt;/a>&lt;/p>
&lt;h1 id="end">End&lt;/h1>
&lt;p>这里没有具体针对特定编程语言来说明时间，时区的用法，不过本质是不变的。
相信找到相应接口所针对的时间，可以很快掌握使用。&lt;/p>- https://dreamanddead.github.io/post/time-in-programming/ - Attribution-NonCommercial-ShareAlike 4.0 International</description></item><item><title>SSH X11 Forward</title><link>https://dreamanddead.github.io/post/ssh-x11-forward/</link><pubDate>Thu, 17 May 2018 20:09:00 +0000</pubDate><guid>https://dreamanddead.github.io/post/ssh-x11-forward/</guid><description>Brain=f(X)? https://dreamanddead.github.io/post/ssh-x11-forward/ -&lt;p>一般情况下，操作远程电脑，ssh+console足矣。突然有一天，隔壁项目组
想要检测下，从香港服务器浏览他们的网页，效果是怎么样的。
背景漆黑语法高亮的控制台好像帮不了忙&amp;hellip;&lt;/p>
&lt;p>诚然，vnc是一个易用可行的方案，但是这里想探索一下，X Window System本身对远程应用的支持。&lt;/p>
&lt;h1 id="x-window-system">X Window System&lt;/h1>
&lt;p>进入主题之前，先简要了解一下X的设计理念。&lt;/p>
&lt;h2 id="cs-architecture">C/S architecture&lt;/h2>
&lt;p>对于linux操作系统来说，GUI只是子系统，像console一样，提供另外一种用户接口。
没有它，核心系统依然正常运转，对于windows用户，这是难以想像的。&lt;/p>
&lt;p>X（X Window System）就是这样一种GUI子系统设计，我们在系统中运行的程序Xorg，是对于设计的一种实现。&lt;/p>
&lt;p>X最突出的特点是，C/S结构。实际运行的GUI程序是Client，连接X Server，与X Server相通信，进行GUI交互操作。&lt;/p>
&lt;p>初听起来有些不可思议，本质上，X Server负责控制鼠标，键盘，屏幕等GUI相关的资源，根据Client的要求，对资源进行
不同的操作。
从某种角度来看，Client与Server通信的内容很像是“从这里到这里绘制一条线” “用这个字体绘制一些文字在这里” 这样的命令，而
Server端绘制屏幕，处理用户交互，读取鼠标键盘事件，告知Client，Client做出相应的反应，再告知Server进行新的绘制。&lt;/p>
&lt;p>这样的设计非常灵活&lt;/p>
&lt;ul>
&lt;li>Client只需要关心与服务器通信的内容，不必关心显卡之类实际的细节&lt;/li>
&lt;li>C/S不必在同一台机器上，通过网络通信，远程运行GUI程序（本文关注的重点）&lt;/li>
&lt;/ul>
&lt;h2 id="window-manager">Window manager&lt;/h2>
&lt;p>使用过GUI的人，都熟悉窗口(window)的概念，对窗口进行最大化，最小化，移动，变换大小等操作也不陌生。
从本质上来讲，窗口不是X Server直接处理的内容。什么意思呢？&lt;/p>
&lt;p>X有一条核心的设计原则&lt;/p>
&lt;blockquote>
&lt;p>provide mechanism, not policy.&lt;/p>
&lt;/blockquote>
&lt;p>从底层来讲，X完全支持你可以设计任何功能（mechanism），但并不强制你用窗口的方式呈现（policy），或许你也有别的方式。
所以说，窗口也只是一种被大家所熟悉的设计方式。&lt;/p>
&lt;p>其实在X中，窗口的功能，是通过一个GUI Client来实现的，管理其它的Client App，一般称其为Window manager。&lt;/p>
&lt;p>在X的设计中，并不包含Window manager。
也因为如此，市面上存在很多Window manager，比如目前kde plasma 5中，kwin作为Window manager。
每个Window manager都对窗口样式，操作方式有独特的理解，用户可以凭借喜好来选择不同的Window manager。&lt;/p>
&lt;h2 id="widget-library">Widget library&lt;/h2>
&lt;p>对于开发者，如果直接写一个GUI App，不会直接从与X交互的通信内容开始。如果那样做，即使绘制一个按钮，过程也是异常复杂的。
通常情况下，使用其它程序库提供的功能，来完成构建GUI App的工作。&lt;/p>
&lt;p>程序库提供了封装，提供通用的Widget单元供开发者调用，这类程序库称之为Widget library or Toolkit。
使用Widget library，只需要调用函数，传递一些参数，就可以得到一个控件，如目录，按钮，滚动条，十分方便程序设计。&lt;/p>
&lt;p>正因如此，Widget library本身的设计，很大程度决定了用户如何使用GUI程序，每个Library的设计也有自己的喜好。
同Window manager一样，Widget library也有很多实现，如著名的Gtk, Qt。&lt;/p>
&lt;h2 id="mess">Mess&lt;/h2>
&lt;p>每个Window manager都有自己管理Client的方式，样式与交互有所差别。
同样的，并没有什么规则，强制开发者一定要用使用什么Toolkit来编写程序，
每个Client使用的Toolkit也有所不同，App表现也不同。&lt;/p>
&lt;p>程序丰富多样的同时，也是一种混乱。这种混乱有很多坏处，比如&lt;/p>
&lt;ul>
&lt;li>用户使用体验不一致&lt;/li>
&lt;li>同时加载很多不同Toolkit编写的程序，会加载不同的Toolkit library到内存，占用内存空间&lt;/li>
&lt;/ul>
&lt;h2 id="desktop-environment">Desktop Environment&lt;/h2>
&lt;p>桌面环境（Desktop Environment）的出现，旨在提供统一的设计原则，对相关差异的地方达成共识，
使用统一的方式来设计程序。著名的DE有Gnome，KDE，XFce等。&lt;/p>
&lt;p>比如KDE，用Qt Toolkit编写，包含Window manager以及其它控制面板，文件管理器等常用功能，用户体验和谐统一。&lt;/p>
&lt;p>由于不同DE使用的底层Toolkit不同，偏向使用相同Toolkit编写的App，
比如&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>KDE&lt;/th>
&lt;th>Gnome&lt;/th>
&lt;th>功能&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>kular&lt;/td>
&lt;td>evince&lt;/td>
&lt;td>pdf阅读器&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>dolphin&lt;/td>
&lt;td>nautilus&lt;/td>
&lt;td>文件管理器&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&amp;hellip;&amp;hellip;&lt;/td>
&lt;td>&amp;hellip;&amp;hellip;.&lt;/td>
&lt;td>&amp;hellip;&amp;hellip;.&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>目前来说，运行与当前DE不同Toolkit编写的App，也没有什么主要问题。
我目前在KDE使用GIMP就很舒服，而且GIMP这么大型的程序，不容易用两种Toolkit写两份。&lt;/p>
&lt;p>如果X提供了policy，就不会有这么多的混乱，但是也会少了相应的多样性。有舍有得。&lt;/p>
&lt;h1 id="remote-connection">Remote connection&lt;/h1>
&lt;p>根据X设计，Server/Client是可以相分离，通过网络进行连接。&lt;/p>
&lt;p>假如机器A，B都运行了X Window System，各自的App都以localhost为X Server地址，
两者分别在各自机器运行程序。&lt;/p>
&lt;figure>
&lt;img src="images/x-remote-connection-a2a-b2b.png"
alt="C/S 本地连接"/> &lt;figcaption>
&lt;p>C/S 本地连接&lt;/p>
&lt;/figcaption>
&lt;/figure>
&lt;p>在机器A，如果App使用B机器的X Server作为X Server地址，
则程序在B机器上运行并显示，由B用户操作。&lt;/p>
&lt;figure>
&lt;img src="images/x-remote-connection-a2b.png"
alt="C/S 远程连接"/> &lt;figcaption>
&lt;p>C/S 远程连接&lt;/p>
&lt;/figcaption>
&lt;/figure>
&lt;h2 id="test-environment">Test environment&lt;/h2>
&lt;p>局域网有两台机器，运行mint与ubuntu&lt;/p>
&lt;ul>
&lt;li>两台机器都安装有X&lt;/li>
&lt;li>ubuntu运行ssh server，mint通过ssh client与之连接&lt;/li>
&lt;li>网络地址如图示&lt;/li>
&lt;/ul>
&lt;figure>
&lt;img src="images/x-remote-connection-mint-ubuntu.png"
alt="内部测试环境"/> &lt;figcaption>
&lt;p>内部测试环境&lt;/p>
&lt;/figcaption>
&lt;/figure>
&lt;p>测试要达到的目标是，将ubuntu上的GUI App，远程运行在mint系统上。&lt;/p>
&lt;h2 id="a-little-theory">A little theory&lt;/h2>
&lt;p>实际操作之前，先消化一些理论 :)&lt;/p>
&lt;h3 id="display">DISPLAY&lt;/h3>
&lt;p>在X Window System中，存在一个关键概念叫做display。
display包含键盘，鼠标，屏幕（screen），由X Server来管理。&lt;/p>
&lt;p>GUI App运行启动，默认选择的display地址存储于DISPLAY变量中。&lt;/p>
&lt;p>变量的格式是&lt;/p>
&lt;ul>
&lt;li>hostname:D.S，X Server通过Tcp端口6000+D来监听服务（D是变量）&lt;/li>
&lt;li>host/unix:D.S，X Server通过本机的UNIX socket来监听服务，一般位于/tmp/.X11-unix/XD（D是变量）&lt;/li>
&lt;li>:D.S，等同于host/unix:D.S&lt;/li>
&lt;/ul>
&lt;p>其中&lt;/p>
&lt;ul>
&lt;li>hostname，主机名，指定主机的地址，也可以为IP&lt;/li>
&lt;li>D，display编号，数字格式&lt;/li>
&lt;li>S，屏幕编号，一个display可管理多个屏幕，默认为0可不写&lt;/li>
&lt;/ul>
&lt;p>一些例子像这样&lt;/p>
&lt;ul>
&lt;li>DISPLAY=light.uni.verse:0&lt;/li>
&lt;li>DISPLAY=localhost:4&lt;/li>
&lt;li>DISPLAY=:0&lt;/li>
&lt;/ul>
&lt;p>除了通过 DISPLAY 变量选择 X Server 地址，部分GUI程序提供 -display 参数，用来指定display地址。
推荐使用 DISPLAY 变量的形式，兼容性更高，比如firefox就没有 -display 选项。&lt;/p>
&lt;pre>&lt;code>$ DISPLAY=10.27.3.9:0 firefox
&lt;/code>&lt;/pre>&lt;h3 id="connect-server">Connect server&lt;/h3>
&lt;p>X Server 可以接受来自任何地方的连接，但是你绝不会想使一个陌生人随意连接，
窃取到你鼠标的移动，键入的内容，毕竟mouse，keyboard也是display的一部分。&lt;/p>
&lt;p>为了安全起见，部分发行版在启动X Server的时候，没有对外开启 X Server 服务，关闭了相应tcp端口，只使用本地unix socket的方式。
如果需要远程，还是需要打开tcp port，修改文件 /etc/X11/xinit/xserverrc，删除&lt;code>-nolisten tcp&lt;/code>参数&lt;/p>
&lt;p>对于本机 mint 使用 sddm 作为 display manager，需要同时修改/etc/sddm.conf，参考&lt;a href="https://wiki.archlinux.org/index.php/SDDM">arch wiki&lt;/a>&lt;/p>
&lt;pre>&lt;code>[XDisplay]
# X server arguments
ServerArguments=-listen tcp
&lt;/code>&lt;/pre>&lt;p>重启之后，就可以发现相应的端口已经打开&lt;/p>
&lt;pre>&lt;code>$ sudo netstat -plunt
......
tcp 0 0 0.0.0.0:6000 0.0.0.0:* LISTEN 1613/Xorg
......
&lt;/code>&lt;/pre>&lt;p>针对安全方面，X Server提供两种访问控制，xhost与xauth，从不同的角度来理解安全问题。&lt;/p>
&lt;h4 id="xhost">xhost&lt;/h4>
&lt;p>xhost的机制非常简单，X Server内部维护一个列表，允许列表内相应的host可以连接X Server，
禁止不在列表内的host连接。&lt;/p>
&lt;p>xhost使用相当简单&lt;/p>
&lt;ul>
&lt;li>xhost，直接列出目前host规则&lt;/li>
&lt;li>xhost +，允许任何host连接&lt;/li>
&lt;li>xhost -，禁止所有host连接&lt;/li>
&lt;li>xhost + host，将一个host添加入允许列表&lt;/li>
&lt;li>xhost - host，相反&lt;/li>
&lt;/ul>
&lt;pre>&lt;code>$ xhost -help
usage: xhost [[+-]hostname ...]
$ xhost
access control enabled, only authorized clients can connect
SI:localuser:zdw
$ xhost +
access control disabled, clients can connect from any host
$ xhost -
access control enabled, only authorized clients can connect
$ xhost + 10.27.2.13
10.27.2.13 being added to access control list
&lt;/code>&lt;/pre>&lt;p>xhost在安全方面进行了一定的控制，但是是从主机的粒度，没有区分用户。
所允许的host上，任何用户都可以连接访问X Server。&lt;/p>
&lt;h4 id="xauth">xauth&lt;/h4>
&lt;p>xauth从另一个角度入手，密钥。只有Client的密钥与X Server的密钥相匹配，才允许连接。&lt;/p>
&lt;p>密钥一般称之为magic cookie，常见的模式是MIT-MAGIC-COOKIE-1。
cookie一般存储于 ~/.Xauthority, 只有用户本身有权限读写。&lt;/p>
&lt;p>xauth 的主要功能就是管理这些 cookie。一般来说，开机，X Server启动，通过&lt;code>-auth&lt;/code>加载cookie文件，
通过查看进程，就可以清楚的看到。&lt;/p>
&lt;p>sddm加载的文件是 ~/.Xauthority，并将其复制到 /var/run/sddm的临时目录下使用。&lt;/p>
&lt;pre>&lt;code>$ sudo ps aux | grep Xorg
root 1775 2.4 1.2 214440 97896 tty7 Ss+ 09:12 0:36 /usr/lib/xorg/Xorg -listen tcp -auth /var/run/sddm/{8c4953d4-ef44-4059-895b-1a9a10b4c5dd} -background none -noreset -displayfd 18 vt7
$ sudo cat /var/run/sddm/\{8c4953d4-ef44-4059-895b-1a9a10b4c5dd\}
zdw-mint0MIT-MAGIC-COOKIE-1�J��~� Z�أ�����
$ cat .Xauthority
zdw-mint0MIT-MAGIC-COOKIE-1�J��~� Z�أ�����
&lt;/code>&lt;/pre>&lt;p>我在实际的测试过程中，在mint端xhost允许所有host连接，xauth暂时没有做配置，
&lt;strong>但是&lt;/strong>ubuntu居然已经可以通过设置DISPLAY的方式，来连接X Server了。&lt;/p>
&lt;p>查看两者的.Xauthority，没有相应的 key 对应，却可以连接。&lt;/p>
&lt;pre>&lt;code>######## mint
$ xauth list
zdw-mint/unix:0 MIT-MAGIC-COOKIE-1 9a4ab5c37eb0205a93d8a386fb8bdccc
######## ubuntu
$ xauth list
lzq/unix:0 MIT-MAGIC-COOKIE-1 f13dcc46e1320b3d156abf22fe53dd7e
$ xauth add 10.27.3.9:0 . 9a4ab5c37eb0205a93d8a386fb8bdccc
$ xauth list
lzq/unix:0 MIT-MAGIC-COOKIE-1 cbdfd1f2858f6e933da074f3d299c7f4
zdw.npl.lan:0 MIT-MAGIC-COOKIE-1 9a4ab5c37eb0205a93d8a386fb8bdccc
$ DISPLAY=10.27.3.9:0 xterm
&lt;/code>&lt;/pre>&lt;p>文档有提到，较新版本的X Server有随时生成cookie的功能，Server生成cookie，并传递给来连接的Client，
达到认证的目的。
但是如果这样，不是失去了xauth本身的目的吗？具体还没有深入考究，TODO。&lt;/p>
&lt;p>上面的示例只是启动了 xterm 程序，流量消耗很小，但是一启动使用firefox，网络带宽达到了10M/s！&lt;/p>
&lt;figure>
&lt;img src="images/x11-remote-firefox-network-load.png"
alt="x11 remote firefox 带宽情况"/> &lt;figcaption>
&lt;p>x11 remote firefox 带宽情况&lt;/p>
&lt;/figcaption>
&lt;/figure>
&lt;p>xauth从密钥的角度入手，如果要保证安全性，重点在于，如何安全的传递X Server cookie的值到相应的Client。
使用telnet, rlogin都是不安全的做法，推荐使用ssh加密传输，安全易行。&lt;/p>
&lt;p>经常只用ssh来传递设置cookie未免过于烦琐，好消息是ssh自身对这种需求有了很好的支持，也到了本文的重点。&lt;/p>
&lt;h1 id="ssh-x11-forward">SSH X11 Forward&lt;/h1>
&lt;p>xhost认证，控制粒度大；xauth认证烦琐，涉及cookie的传输与设置。
实际在部署中，往往涉及防火墙的相关设置。
更重要的是，X Remote App本身的流量是未加密的，有很大的被监听的风险。&lt;/p>
&lt;p>ssh提供了一种安全方便的方式，就是X11 Forward，来解决上述问题。&lt;/p>
&lt;h2 id="quick-start">Quick start&lt;/h2>
&lt;p>先来快速体验一下 X11 Forward。
在ssh server端，文件/etc/ssh/sshd_config中，配置&lt;/p>
&lt;pre>&lt;code>X11Forwarding yes
X11DisplayOffset 10
&lt;/code>&lt;/pre>&lt;p>在ssh client端，可以在连接时加上&lt;code>-X&lt;/code>选项，或者在~/.ssh/config中，对Host加上配置&lt;/p>
&lt;pre>&lt;code>Host 10.27.2.13
ForwardX11 yes
&lt;/code>&lt;/pre>&lt;p>在我们的示例中，mint作为ssh client，ubuntu作为ssh server，配置了相应选项之后，
ssh连接，在ssh console session中运行&lt;/p>
&lt;pre>&lt;code>$ xterm
&lt;/code>&lt;/pre>&lt;p>xterm就出现在mint主机，不需要任何多余的设置，ssh全部帮我们做好，amazing～&lt;/p>
&lt;h2 id="what-ssh-does">What ssh does&lt;/h2>
&lt;p>在上面简单的设置中，就实现了X Remote App的功能，不禁觉得神奇，ssh到底做了什么？&lt;/p>
&lt;p>其实X机制本身是不变的，像上面讨论的一样，只是ssh帮我们做了相关连接所需要的设置。
在上面的示例中，mint通过ssh连接ubuntu，
这里查看 DISPLAY，端口&lt;/p>
&lt;pre>&lt;code>$ echo $DISPLAY
localhost:10.0
$ sudo netstat -plunt
Proto Recv-Q Send-Q Local Address Foreign Address State PID/Program name
tcp 0 0 127.0.0.1:6010 0.0.0.0:* LISTEN 20093/8
&lt;/code>&lt;/pre>&lt;p>DISPLAY的值是 localhost:10.0 ，根据前面章节对 DISPLAY 值的分析， D=10，
所以在本地监听的端口是 6000+10=6010。&lt;/p>
&lt;p>为什么会是10呢？这就和上面 sshd_config 设置有关。&lt;/p>
&lt;pre>&lt;code>X11DisplayOffset 10
&lt;/code>&lt;/pre>&lt;p>意思是ssh从D=10开始定义新的 DISPLAY，避免与本机上的原有的 DISPLAY 定义相冲突。&lt;/p>
&lt;p>这次连接的示意图如下&lt;/p>
&lt;figure>
&lt;img src="images/ssh-x11-forward.png"
alt="ssh x11 forward 图示 from oreilly"/> &lt;figcaption>
&lt;p>ssh x11 forward 图示 from oreilly&lt;/p>
&lt;/figcaption>
&lt;/figure>
&lt;p>ssh client请求X forward，如果sshd允许，则在本机开启 X proxy server，并且设置了 DISPLAY 变量为 X proxy server，
代理所有X流量，通过ssh，传递到client端的6000（没有开启tcp端口也可以，待测试）&lt;/p>
&lt;h2 id="ssh-auth-spoof">SSH auth spoof&lt;/h2>
&lt;p>ssh通过流量转发，使得 X Client 可以远程连接 X Server，但是还没有讲清楚的是认证的问题，
ssh X11 forward是如何处理安全认证的。&lt;/p>
&lt;p>先从xhost来说，X11 Forward完全绕过xhost的控制，因为X11 Forward本质是通过流量代理转发。
也就是说，如果在mint主机上，禁止所有host连接自身的X，ubuntu主机依然可以通过X11 Forward，
在mint主机上远程运行App。&lt;/p>
&lt;p>其次就是xauth，
先来看连接ssh之后，两台主机的cookie list。&lt;/p>
&lt;pre>&lt;code>###### ubuntu
$ xauth list
lzq/unix:10 MIT-MAGIC-COOKIE-1 0377b5696a856c06f76fd20dc3ed837f
###### mint
$ xauth list
zdw-mint/unix:0 MIT-MAGIC-COOKIE-1 26a01cda76d69ab6a1f39b90a2fbc48b
&lt;/code>&lt;/pre>&lt;p>只有本地的条目，而且两者完全不同，难道ssh也绕过了xauth机制？
其实ssh通过一种聪明的方式来传递auth cookie，称为authentication spoofing。&lt;/p>
&lt;figure>
&lt;img src="images/ssh-auth-spoof.gif"
alt="ssh如何处理auth认证 from oreilly"/> &lt;figcaption>
&lt;p>ssh如何处理auth认证 from oreilly&lt;/p>
&lt;/figcaption>
&lt;/figure>
&lt;p>其中的关键就在于，有两个不同的cookie。&lt;/p>
&lt;p>X Server本身的cookie这里称为 real display key，其中ssh client
会生成一个同样格式的cookie，称为 fake display key。&lt;/p>
&lt;ol>
&lt;li>X Server real display key&lt;/li>
&lt;li>ssh client 加载 real display key&lt;/li>
&lt;li>随机生成一个相同长度的 fake display key&lt;/li>
&lt;li>将 fake display key 和其类型发送到 ssh server, 通过 xauth 建立 X proxy server 与 fake display key 的关联&lt;/li>
&lt;li>X client 开始与 X Proxy Server 通信，读取对应的 fake display key&lt;/li>
&lt;li>X proxy server通过ssh，传输X流量，第一个包有特殊处理&lt;/li>
&lt;li>X 流量转发到 ssh client&lt;/li>
&lt;li>ssh client读取第一个数据包中的 fake display key，与 real display key 相对比&lt;/li>
&lt;li>如果不匹配，则连接失败；如果匹配，将包中的Key替换为 real display key&lt;/li>
&lt;li>流量传递给 X server，X Server从中读取到key（没有意识到有swap key的过程）&lt;/li>
&lt;li>（xauth过程）如果不匹配，则连接失败；如果与real display key匹配，则认证成功，连接建立&lt;/li>
&lt;/ol>
&lt;p>这样的过程看似复杂，其实很巧妙&lt;/p>
&lt;ul>
&lt;li>不使用xhost这样不安全的方式&lt;/li>
&lt;li>安全的传递cookie&lt;/li>
&lt;li>全部X流量加密&lt;/li>
&lt;li>如果ssh server不可信，过程中也没有暴露真实的cookie&lt;/li>
&lt;/ul>
&lt;p>What a clever way!&lt;/p>- https://dreamanddead.github.io/post/ssh-x11-forward/ - Attribution-NonCommercial-ShareAlike 4.0 International</description></item><item><title>Serve http and https at same time with nginx</title><link>https://dreamanddead.github.io/post/serve-http-https-same-time/</link><pubDate>Thu, 17 May 2018 14:30:00 +0000</pubDate><guid>https://dreamanddead.github.io/post/serve-http-https-same-time/</guid><description>Brain=f(X)? https://dreamanddead.github.io/post/serve-http-https-same-time/ -&lt;p>之前讨论过，&lt;a href="https://dreamanddead.github.io/2018/04/11/comfortable-way-when-testing-https-site.html">网站从http迁移到https时，需要注意些什么&lt;/a>。
由于旧有的服务依旧在运行，需要同时保留http与https。
突然有一天，访问http服务，页面上只出现nginx的错误信息：&lt;/p>
&lt;pre>&lt;code>400 bad request the plain http request was sent to https port
&lt;/code>&lt;/pre>&lt;p>这让我觉得非常奇怪，按照之前的理解，这样设置应该是没有问题的。&lt;/p>
&lt;div class="highlight">&lt;pre style="background-color:#eed;-moz-tab-size:4;-o-tab-size:4;tab-size:4">&lt;code class="language-nginx" data-lang="nginx">&lt;span style="color:#8b008b;font-weight:bold">server&lt;/span> {
&lt;span style="color:#8b008b;font-weight:bold">listen&lt;/span> &lt;span style="color:#b452cd">80&lt;/span>;
&lt;span style="color:#8b008b;font-weight:bold">listen&lt;/span> &lt;span style="color:#b452cd">443&lt;/span>;
&lt;span style="color:#8b008b;font-weight:bold">ssl&lt;/span> &lt;span style="color:#00688b">on&lt;/span>;
&lt;span style="color:#8b008b;font-weight:bold">ssl_certificate&lt;/span> &lt;span style="color:#cd5555">/etc/nginx/ssl/domain.crt&lt;/span>;
&lt;span style="color:#8b008b;font-weight:bold">ssl_certificate_key&lt;/span> &lt;span style="color:#cd5555">/etc/nginx/ssl/domain.key&lt;/span>;
&lt;span style="color:#8b008b;font-weight:bold">.........&lt;/span>
&lt;span style="color:#cd5555">.........&lt;/span>
&lt;span style="color:#cd5555">.........&lt;/span>
&lt;span style="color:#a61717;background-color:#e3d2d2">}&lt;/span>
&lt;/code>&lt;/pre>&lt;/div>&lt;p>后来按照错误信息进行一番搜索，还是漏掉了一些细节。&lt;/p>
&lt;h1 id="trouble-shoot">trouble shoot&lt;/h1>
&lt;p>从 &lt;a href="https://stackoverflow.com/questions/8768946/dealing-with-nginx-400-the-plain-http-request-was-sent-to-https-port-error">stackoverflow&lt;/a> 上搜索得到的结果，
发现自己对于 ssl 的配置理解还不够充分。&lt;/p>
&lt;p>一般在nginx配置中，ssl 可能出现在两个地方&lt;/p>
&lt;ul>
&lt;li>在 listen 指令之后，作为参数&lt;/li>
&lt;li>在 server 块下，作为指令，参数为 on/off&lt;/li>
&lt;/ul>
&lt;p>对于 ssl 指令，&lt;a href="http://nginx.org/en/docs/http/ngx_http_ssl_module.html#ssl">官方文档&lt;/a> 已经不推荐使用，而是推荐使用 listen 指令加 ssl 参数的格式。&lt;/p>
&lt;p>listen 指令的 &lt;a href="http://nginx.org/en/docs/http/ngx_http_core_module.html#listen">ssl 参数&lt;/a> 的作用是，标明 listen 的端口应该运行 ssl 模式。
同时托管 http 与 https时，推荐&lt;a href="http://nginx.org/en/docs/http/configuring_https_servers.html#single_http_https_server">使用如下配置&lt;/a>：&lt;/p>
&lt;div class="highlight">&lt;pre style="background-color:#eed;-moz-tab-size:4;-o-tab-size:4;tab-size:4">&lt;code class="language-nginx" data-lang="nginx">&lt;span style="color:#8b008b;font-weight:bold">server&lt;/span> {
&lt;span style="color:#8b008b;font-weight:bold">listen&lt;/span> &lt;span style="color:#b452cd">80&lt;/span>;
&lt;span style="color:#8b008b;font-weight:bold">listen&lt;/span> &lt;span style="color:#b452cd">443&lt;/span> &lt;span style="color:#cd5555">ssl&lt;/span>;
&lt;span style="color:#8b008b;font-weight:bold">ssl&lt;/span> &lt;span style="color:#00688b">off&lt;/span>;
&lt;span style="color:#8b008b;font-weight:bold">ssl_certificate&lt;/span> &lt;span style="color:#cd5555">/etc/nginx/ssl/domain.crt&lt;/span>;
&lt;span style="color:#8b008b;font-weight:bold">ssl_certificate_key&lt;/span> &lt;span style="color:#cd5555">/etc/nginx/ssl/domain.key&lt;/span>;
&lt;span style="color:#8b008b;font-weight:bold">.........&lt;/span>
&lt;span style="color:#cd5555">.........&lt;/span>
&lt;span style="color:#cd5555">.........&lt;/span>
&lt;span style="color:#a61717;background-color:#e3d2d2">}&lt;/span>
&lt;/code>&lt;/pre>&lt;/div>&lt;p>所以问题的核心就在于，指令 &lt;code>ssl on&lt;/code>，将所有 listen 的端口，都运行在 ssl 模式，但是对于端口 80，
这样明显是不对的，也就导致了最开始的报错信息。&lt;/p>
&lt;pre>&lt;code>400 bad request the plain http request was sent to https port
&lt;/code>&lt;/pre>&lt;p>这时就推荐使用 listen 加 ssl 参数，更细粒度的控制哪些端口运行在 ssl 模式下。&lt;/p>- https://dreamanddead.github.io/post/serve-http-https-same-time/ - Attribution-NonCommercial-ShareAlike 4.0 International</description></item><item><title>thinkpad E530c 的升级之路</title><link>https://dreamanddead.github.io/post/upgrade-thinkpad-e530c/</link><pubDate>Sat, 12 May 2018 20:10:00 +0000</pubDate><guid>https://dreamanddead.github.io/post/upgrade-thinkpad-e530c/</guid><description>Brain=f(X)? https://dreamanddead.github.io/post/upgrade-thinkpad-e530c/ -&lt;p>不知不觉，自己的&lt;a href="http://detail.zol.com.cn/361/360255/param.shtml">Thinkpad E530c&lt;/a>入手5年，期间换过数次系统，如今不觉有些老态龙钟，
硬件资源已远远不足以支持日常使用的需求，速度慢多次卡顿，十分损伤工作效率。&lt;/p>
&lt;p>决心升级一下跟随多年的“小黑”，焕发新颜。&lt;/p>
&lt;h1 id="拆机">拆机&lt;/h1>
&lt;p>拆机之前，&lt;strong>一定要先取下电池！！！&lt;/strong>，不然容易因为短路之类损伤内部元器件，
后果很严重。&lt;/p>
&lt;p>拆机只需要一把小型的十字螺丝刀，和一字螺丝刀。&lt;/p>
&lt;p>拆机只需要拆背面就可以，可以更换的主要零件都在这里，不必将电脑拆到芯片级别。&lt;/p>
&lt;figure>
&lt;img src="images/tp-panel.jpg"/>
&lt;/figure>
&lt;ul>
&lt;li>从1,2,拆下电源&lt;/li>
&lt;li>从5,6,7,松开螺丝，从8处撬开面板&lt;/li>
&lt;/ul>
&lt;figure>
&lt;img src="images/tp-panel-inside.jpg"/>
&lt;/figure>
&lt;p>主要零件分布&lt;/p>
&lt;ul>
&lt;li>A处风扇&lt;/li>
&lt;li>B处内存&lt;/li>
&lt;li>C处是CPU&lt;/li>
&lt;li>D处是硬盘&lt;/li>
&lt;li>E处是无线网卡&lt;/li>
&lt;li>F处是显卡&lt;/li>
&lt;/ul>
&lt;h1 id="硬盘">硬盘&lt;/h1>
&lt;p>一般硬盘IO速率，远远小于内存读写的速度，
提升硬盘IO速率，缩短与内存读写速率的差距，
对于性能提升，是非常明显的。
计划将原有的HDD，更换为SSD。&lt;/p>
&lt;p>可以安装硬盘的有两个位置，一个是原先的硬盘位，另一个是光驱位。
一般来讲，硬盘位更稳定，速度更快，所以决定在更换HDD的时候，
将SSD安装在原有硬盘位，HDD放到光驱位，光驱将废弃不用。&lt;/p>
&lt;figure>
&lt;img src="images/tp-panel-inside.jpg"/>
&lt;/figure>
&lt;ul>
&lt;li>从1,2,处，松开螺丝，向外取出硬盘&lt;/li>
&lt;/ul>
&lt;figure>
&lt;img src="images/tp-disk.jpg"/>
&lt;/figure>
&lt;p>一般硬盘的侧面与背面，都有4个螺丝孔（1,2是侧面，3,4是背面）。
在取下原有硬盘之后，将原来的硬盘支架取下来（从1,2螺丝及对面的两颗螺丝），
换上新硬盘，固定支架，再安装回硬盘位。&lt;/p>
&lt;p>在更换之后，开机关机秒速，体验很赞。&lt;/p>
&lt;h1 id="光驱">光驱&lt;/h1>
&lt;p>根据平时的使用经验，光驱基本使用不到，决定将旧硬盘放置到光驱处。
有一种顾虑是，这样内部的散热会不好，因为SSD安装了系统，HDD用于附加存储，使用到的概率很小，平时使用应该不是问题。&lt;/p>
&lt;figure>
&lt;img src="images/tp-panel.jpg"/>
&lt;/figure>
&lt;ul>
&lt;li>松开3处的螺丝&lt;/li>
&lt;li>再从4处将光驱拔出来&lt;/li>
&lt;/ul>
&lt;p>因为光驱位的接口与硬盘位的接口不同，虽然都为SATA口，但是有一些区别，所以在将硬盘安装到光驱位
的时候，需要 光驱硬盘支架。&lt;/p>
&lt;figure>
&lt;img src="images/tp-disk-cdrom-interface.jpg"/>
&lt;/figure>
&lt;p>光驱位接口&lt;/p>
&lt;figure>
&lt;img src="images/tp-disk-interface.jpg"/>
&lt;/figure>
&lt;p>硬盘位接口&lt;/p>
&lt;figure>
&lt;img src="images/tp-disk-cdrom.jpg"/>
&lt;/figure>
&lt;p>左边是原来的光驱，右边是光驱硬盘支架。
首先将硬盘安装到 光驱硬盘支架 上，记得将原有光驱位1处的螺丝与2处的外壳，换到新的 光驱硬盘支架 上。
将 光驱硬盘支架 装回原来的光驱位即可。&lt;/p>
&lt;h1 id="风扇">风扇&lt;/h1>
&lt;p>笔记本原来的风扇不转动，积灰严重，
可以在网上根据对应型号，购买一个新的。&lt;/p>
&lt;p>新风扇运转流畅，声音小，散热变得更好。&lt;/p>
&lt;figure>
&lt;img src="images/tp-panel-inside.jpg"/>
&lt;/figure>
&lt;ul>
&lt;li>将17处的插口拔下&lt;/li>
&lt;li>从7,8,9处将螺丝松开，取下风扇&lt;/li>
&lt;li>换上新风扇&lt;/li>
&lt;/ul>
&lt;h1 id="cpu">CPU&lt;/h1>
&lt;p>平时日常使用，开启Firefox，CPU都要跑满40%，开启关闭标签页时常卡住，
下一步决定主要更换的是CPU。&lt;/p>
&lt;p>一般CPU型号，以M（Mobile）结束的，是可以更换的，以U结尾的是焊接在主板上的，不可更换。
E530c的CPU是i5-3210M，lucky。&lt;/p>
&lt;p>CPU不是随意更换的，要根据主板型号与功耗，选择合适的。&lt;/p>
&lt;ul>
&lt;li>从cpu-upgrade网站，根据主板型号，找到可以适配的所有CPU。E530c的主板型号是&lt;a href="http://www.cpu-upgrade.com/mb-Intel_(chipsets)/HM77_Express.html">HM77&lt;/a>，可升级的最高系列是i7-3840QM。&lt;/li>
&lt;li>从功耗来讲，原来的CPU是35W，i7-3840QM是45W，功率相同是最好，如果强制为了高配置，而强制换上高功率CPU，可能会散热不足，损坏内部元件，所以一定要保持散热。&lt;/li>
&lt;/ul>
&lt;figure>
&lt;img src="images/tp-panel-inside.jpg"/>
&lt;/figure>
&lt;ul>
&lt;li>CPU在散热片的下面，要先取下风扇，再取下散热片，其次才能取下CPU&lt;/li>
&lt;li>取下风扇&lt;/li>
&lt;li>松下10,11,12,13,14,15,16的螺丝，取下散热片&lt;/li>
&lt;/ul>
&lt;figure>
&lt;img src="images/tp-cpu.jpg"/>
&lt;/figure>
&lt;ul>
&lt;li>将1处的一字螺丝，逆时针180度，再取下CPU&lt;/li>
&lt;li>更换新CPU&lt;/li>
&lt;li>顺时针180紧上&lt;/li>
&lt;/ul>
&lt;p>对于更换CPU，还有重要的一点是，一手市面上只有针对PC的CPU售卖，
如果要买到笔记本CPU，只能通过非官方的途径，而这些途径得来的CPU并不能保证
其质量，其中关注最多的就是ES/QS版本的CPU。&lt;/p>
&lt;p>简单的说，ES/QS是工程的测试版，没有足够的稳定性，理论上不应该流通在市面上。
具体可参考 &lt;a href="http://ask.zol.com.cn/q/23131.html">CPU ES版、QS版和正式版的区别&lt;/a>&lt;/p>
&lt;p>因为是linux系统，没有cpuz这样的工具，cpu的信息中也看不出是否是正式版。
试用的几天，感觉速度还可以。如果有知道方法的人，感谢可以告诉我 :)&lt;/p>
&lt;pre>&lt;code>$ lscpu
Architecture: x86_64
CPU 运行模式： 32-bit, 64-bit
Byte Order: Little Endian
CPU(s): 8
On-line CPU(s) list: 0-7
每个核的线程数：2
每个座的核数： 4
Socket(s): 1
NUMA 节点： 1
厂商 ID： GenuineIntel
CPU 系列： 6
型号： 58
Model name: Intel(R) Core(TM) i7-3840QM CPU @ 2.80GHz
步进： 9
CPU MHz： 1200.732
CPU max MHz: 3800.0000
CPU min MHz: 1200.0000
BogoMIPS: 5587.10
虚拟化： VT-x
L1d 缓存： 32K
L1i 缓存： 32K
L2 缓存： 256K
L3 缓存： 8192K
NUMA node0 CPU(s): 0-7
Flags: fpu vme de pse tsc msr pae mce cx8 apic sep mtrr pge mca cmov pat pse36 clflush dts acpi mmx fxsr sse sse2 ss ht tm pbe syscall nx rdtscp lm constant_tsc arch_perfmon pebs bts rep_good nopl xtopology nonstop_tsc aperfmperf pni pclmulqdq dtes64 monitor ds_cpl vmx smx est tm2 ssse3 cx16 xtpr pdcm pcid sse4_1 sse4_2 x2apic popcnt tsc_deadline_timer aes xsave avx f16c rdrand lahf_lm epb tpr_shadow vnmi flexpriority ept vpid fsgsbase smep erms xsaveopt dtherm ida arat pln pts
&lt;/code>&lt;/pre>&lt;h1 id="内存">内存&lt;/h1>
&lt;p>目前有两个插槽，最初是4G内存，后来加装了一条4G，共8G现在，
目前还够用。&lt;/p>
&lt;p>听说这个型号有内存单条8G的限制，还没有验证过，如果是那样，最多升级到16G内存。&lt;/p>
&lt;figure>
&lt;img src="images/tp-panel-inside.jpg"/>
&lt;/figure>
&lt;ul>
&lt;li>3,4,5,6是4个卡位，向外掰开，内存就会弹出&lt;/li>
&lt;li>安装内存时，对准插槽，再按下即可&lt;/li>
&lt;/ul>
&lt;h1 id="显示器">显示器&lt;/h1>
&lt;p>目前屏幕的分辨率为1366×768，本来想更换为1080P ips屏，
但是根据资料并联系商家，没有相应能适配的屏幕，有些可惜。&lt;/p>
&lt;p>不过目前一直使用外接显示器，这一点也可以接受。&lt;/p>
&lt;h1 id="电池">电池&lt;/h1>
&lt;p>使用电脑的时间这么多年，电池的续航能力也变差了。&lt;/p>
&lt;p>直接在网上搜索对应型号，买原装的可以，简单有效。&lt;/p>- https://dreamanddead.github.io/post/upgrade-thinkpad-e530c/ - Attribution-NonCommercial-ShareAlike 4.0 International</description></item></channel></rss>